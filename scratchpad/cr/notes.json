[
 {
  "file": "src/fj/frame_render.fj",
  "line": 2007,
  "rule": "R6",
  "severity": "design",
  "claim": "lines_sky_base omits the oracle's `& (tw-1)` mask (ReferenceModel.sky_base, reference_model.py:2186), so skb is the unmasked 8-bit (viewangle>>24) in [0,255], and the load-bearing comment here ('both addends are <= 127 ... the bank holds 256 lists ... max 253') states a false invariant: byte-exactness actually rests on the undocumented chain [2-nibble add wraps mod 256] \u2218 [256 is a multiple of tw=128] \u2218 [bank holds SKY_BANK_MUL*tw = 384 lists whose entry u bakes column u % tw (sky_texel_u, reference_model.py:2200)].",
  "scenario": "Any viewpoint facing the south half of the angle space (viewangle >= 0x80000000) makes skb in [128,255] and the u sum reach up to 382, wrapping mod 256 \u2014 correct today only by the mod-128 congruence. A maintainer who trusts the comment (or the emitter docstring at wall_renderer.py:2018, which says the bank holds 2*tw lists while SKY_BANK_MUL=3) and shrinks the bank below 256 lists, or swaps in a sky texture whose width does not divide 256, silently breaks sky columns for half of all view angles \u2014 a class of change the 7-viewpoint gate would only catch if a gate viewpoint happens to face that way with sky visible. Fix is either a comment correction stating the real invariant, or adding the mask to mirror the oracle formula bit-for-bit.",
  "needs_code_change": false,
  "unit": "fj-frame_render",
  "verdict": "confirmed",
  "v_needs_change": false,
  "v_reason": "Verified against the working tree: lines_sky_base (frame_render.fj:1992-1998) yields skb=(viewangle>>24)&0xFF in [0,255] with no tw-1 mask anywhere before the 2-nibble add (lines 2014/2032/2562-2564), while the oracle masks &(tw-1) (reference_model.py:2186). The comment at frame_render.fj:2006-2007 (\"both addends <= 127... bank holds 256 lists... max 253\") is false: skb reaches 255 for viewangle >= 0x80000000 (half the angle space) and the bank holds SKY_BANK_MUL*tw=384 lists (wall_renderer.py:2003,2056). Byte-exactness rests exactly on the undocumented chain the reviewer states: 2-nibble add wraps mod 256, 128 divides 256, bank entries bake u%tw via sky_texel_u (reference_model.py:2200). The emitter docstrings are also wrong as cited: wall_renderer.py:2015 claims a mask the fj lacks, 2018-2019 claim \"2*tw lists\" and \"both addends in [0,tw-1]\" contradicting SKY_BANK_MUL=3 five lines above; same stale claim at 1552. This fully explains certification passing \u2014 the congruence holds for ALL viewangles, so no reachable input misrenders; the defect is purely documentary. The maintainer-hazard scenario (shrink the bank trusting the comment, or restructure the sum) is real, though the texture-swap leg is weak (any pow2 width <= 256 divides 256). needs_code_change=false per the contract: shipped behavior is working-as-certified for every input; the fix is a comment correction (or an optional 1-op mask for literal R6 mirroring) \u2014 a design/doc note, not a latent runtime defect."
 },
 {
  "file": "src/fj/frame_render.fj",
  "line": 1668,
  "rule": "R6",
  "severity": "design",
  "claim": "ts_step_faces takes slotstride as a parameter and uses it for the per-column advance (hex.ptr_add fbp, slotstride, line 1802) but computes the initial slot address with a hardcoded x16 whole-nibble shift (hex.shl_hex w/4, 1, fidx), and the read half (lines_step_load ~2098, lines_steps_load2 ~2196, lines_spr_load ~2269) hardcodes the same shift without taking the parameter at all; thing_record_body's rep(slotstride/16) shl-by-one-nibble (line 1422) is only correct for slotstride=16.",
  "scenario": "Change STEP_SLOT_STRIDE or SPR_SLOT_STRIDE in config (e.g. 16 -> 32 to hold a third piece): the ptr_add advance moves 32 bytes per column while the initial index and every reader still multiply x by 16 (and thing_record_body's rep would multiply by 256), so records land at wrong addresses from column x1+1 onward -- silent cross-column slot corruption instead of a build error.",
  "needs_code_change": false,
  "unit": "fj-frame_render",
  "verdict": "confirmed",
  "v_needs_change": false,
  "v_reason": "Verified in src/fj/frame_render.fj: ts_step_faces takes slotstride but only line 1802 (ptr_add fbp, slotstride) honors it; the initial index (line 1668) is a hardcoded one-nibble shift (x16), the readers at lines 2104, 2197, 2270 hardcode the same shift without any stride parameter, and thing_record_body line 1422 uses rep(slotstride/16) one-nibble shifts, which equals x slotstride only for slotstride=16 (even the \"power of 16\" value 256 documented at wall_renderer.py:1831 would compute 16^16). Both config constants (STEP_SLOT_STRIDE, SPR_SLOT_STRIDE at wall_renderer.py:1831/1888) are 16, so shipped/certified behavior is consistent and the defect is latent-on-config-edit only: changing the stride silently corrupts slot addressing from column x1+1 onward instead of failing the build. Genuine R6 single-source-of-truth violation, but a working-as-certified robustness/design finding \u2014 no reachable input or knob triggers it \u2014 so needs_code_change=false (recommend a build-time assert stride==16 or removing the misleading parameter as a follow-up)."
 },
 {
  "file": "src/fj/frame_render.fj",
  "line": 796,
  "rule": "R6",
  "severity": "design",
  "claim": "The 11-dw col_struct stride in seg_pass1_leaf_body_stream is a bare literal duplicated at four sites (mul_const ... 11 at 796 and 893, ptr_add cptr, 11 at 804 and 902) plus an implicit fifth copy as the invariant that the drawn+5-field write chain advances exactly 11 dw -- there is no single named constant tying them together.",
  "scenario": "Add one 2-nibble field to the packed column struct (e.g. a second lit byte) and update the write chain but miss one of the two scan loops: the occlusion pre-scan then reads drawn[] at a stride of 11 over a 13-dw struct, misreading field bytes as drawn flags -- segs get skipped as 'fully occluded' or scanned past the array with no assembler or runtime diagnostic.",
  "needs_code_change": false,
  "unit": "fj-frame_render",
  "verdict": "confirmed",
  "v_needs_change": false,
  "v_reason": "Verified against the working tree: in src/fj/frame_render.fj the 11-dw col_struct stride appears as a bare literal at exactly the four claimed sites \u2014 `hex.mul_const w/4, sidx, x1, 11` at lines 796 and 893, and `hex.ptr_add cptr, 11` at lines 804 and 902 \u2014 and the write chain at lines 928-938 advances exactly 11 dw (1 write_byte_and_inc + 10 write_hex_and_inc), which is the implicit fifth copy the claim describes (the comment at 925 even states '11 dw advanced == the struct stride' as prose, not a checked constant). No named constant ties them: the macro signature (line 769) parameterizes band_stride but not the column stride, even though the fj macro language demonstrably supports compile-time constant expressions here (e.g. `(1+band_stride)*dw` at 828/860), so a named stride was clearly feasible. The claim is actually UNDERSTATED: the emit-side consumer in src/doomfj/wall_renderer.py lines 1175-1176 hardcodes the same layout independently (`col_struct + {11*x + 1}*dw` ... `{11*x + 9}*dw`), so the failure scenario (add a field, update the write chain, miss a scan loop or the Python _cell offsets \u2192 drawn[] read at stride 11 over a 13-dw struct, field bytes misread as drawn flags, segs silently skipped as occluded, no assembler or runtime diagnostic) spans two files, which is exactly the R6 cross-boundary mirror concern. Certification is not contradicted: today all copies agree, so behavior is byte-exact \u2014 this is purely a latent maintenance hazard. needs_code_change=false because it is a severity=design SSOT/hygiene finding with no present or latent misbehavior on any input, and the affected leaf (seg_pass1_leaf_body_stream, raster_mode 'stream'/'spans') is not the shipped 'lines' raster path \u2014 per the review contract, design notes get false."
 },
 {
  "file": "src/fj/frame_render.fj",
  "line": 1161,
  "rule": "none",
  "severity": "style",
  "claim": "Two orphaned doc headers sit directly above thing_record_body: the 'M13-lines -- the DUMB-DEVICE leaf body' block (1161-1169) describes the splitxb lines leaves defined at 2407+, and the 'M13-2S rung 3a -- the TWO-SIDED PLANE-CLAIM leaf' block (1170-1187) describes seg_pass1_leaf_body_ts defined at 1846 (which consequently has no header of its own), so the comments lie about the macro they annotate.",
  "scenario": "A reader (or a future review/refactor session) looking up seg_pass1_leaf_body_ts at 1846 finds no contract comment, while a reader of thing_record_body first wades through ~40 lines describing two unrelated leaves -- e.g. the tsstop/budget semantics documented at 1170-1187 are easy to miss when editing the ts leaf itself, exactly the state-machine subtlety (the (1210,1187) field flip) that already bit once.",
  "needs_code_change": false,
  "unit": "fj-frame_render",
  "verdict": "confirmed",
  "v_needs_change": false,
  "v_reason": "Verified in C:\\Users\\tomhe\\Documents\\doom-flipjump\\src\\fj\\frame_render.fj: two orphaned headers sit directly above thing_record_body (def at 1200) \u2014 the \"M13-lines DUMB-DEVICE\" block (1161-1169) describes the lines leaves (seg_pass1_leaf_body_lines at 2412 / seg_pass2_leaf_body_lines at 2456, the only caller of stream.emit_col_lines it references, line 2718), and the \"M13-2S rung 3a\" block (1170-1187) describes seg_pass1_leaf_body_ts, defined header-less at 1846 (1845 blank, 1844 closes the prior macro). The correct thing_record_body header is the third stacked block (1188-1199). The 1170-1187 block even documents the tsstop claim-complete semantics that the P2b two-tier-stop fix has since revised, compounding the misdirection. Purely a comment-placement/doc issue \u2014 no runtime effect, certification unaffected \u2014 so per the review contract this is a style note, not a code change."
 },
 {
  "file": "src/fj/projection.fj",
  "line": 217,
  "rule": "none",
  "severity": "style",
  "claim": "proj.coarse_bracket (:217) and proj.frustum_sector (:325) are dead code \u2014 no emitter or test instantiates them, their baked `coarse_bounds` table is never emitted (generate_coarse_bounds_fj is imported at wall_renderer.py:43 but never called), and wall_renderer.py:519's comment records the coarse-cull pre-pass was priced at +24.7M and abandoned.",
  "scenario": "Uninstantiated fj defs are never assembled or type-checked, so these ~170 lines rot silently if any hex.* signature they use changes; a future maintainer reading their still-authoritative doc-comments ('provably invisible -> cullable') wires them in believing they are live, tested logic and inherits whatever has rotted plus the un-audited eye-on-vertex degenerate case.",
  "needs_code_change": false,
  "unit": "fj-projection",
  "verdict": "confirmed",
  "v_needs_change": false,
  "v_reason": "Verified in the working tree: proj.coarse_bracket (src/fj/projection.fj:217) and proj.frustum_sector (:325) have zero instantiations in any tracked .fj file, emitter, or tests/ (the only exerciser, scratchpad/test_coarse_bracket_fj.py, is untracked and covers only coarse_bracket); generate_coarse_bounds_fj is imported at src/doomfj/wall_renderer.py:43 with no call site, so the coarse_bounds table the macro reads (projection.fj:295/297) is never emitted; wall_renderer.py:519 documents the pre-pass was priced at +24.7M and abandoned. Since fj defs assemble only on instantiation, the ~170 lines are outside every certification gate and their still-authoritative doc-comments can rot silently \u2014 the claim holds. It is dead code with no effect on the certified binary, so per the review contract this is a style/dead-code note (needs_code_change=false), though deleting or explicitly marking the block abandoned would be cheap insurance."
 },
 {
  "file": "src/fj/projection.fj",
  "line": 204,
  "rule": "R8",
  "severity": "style",
  "claim": "Rewrite leftovers: point_to_angle's `czero` local (:101 declaration, :204 data cell) is dead since the hex.if0 conversion of the zero checks, and project_thing's `ok2` label (:1439, :1524) is declared and defined but never jumped to.",
  "scenario": "No runtime effect (8 nibbles of dead data, one dead label); the cost is reader confusion \u2014 `czero: hex.vec 8, 0` implies a compare that no longer exists, and `ok2` implies a reject path that can branch there when nothing does.",
  "needs_code_change": false,
  "unit": "fj-projection",
  "verdict": "confirmed",
  "v_needs_change": false,
  "v_reason": "Verified in C:\\Users\\tomhe\\Documents\\doom-flipjump\\src\\fj\\projection.fj: point_to_angle's `czero` (declared line 101, data cell line 204) is unreferenced in its macro body \u2014 the zero checks at lines 126/129 use hex.if0; every other czero hit in the file belongs to a different macro's own used local. project_thing's `ok2` (declared line 1439, defined line 1524) appears nowhere else \u2014 no `;ok2` and no compare targets it; it is reached only by fall-through after .scale_recip_div. Both are dead rewrite leftovers with zero runtime effect (8 nibbles of inert data, one no-op label), consistent with byte-exact certification. Style/hygiene only, so no code change is warranted now."
 },
 {
  "file": "src/fj/projection.fj",
  "line": 384,
  "rule": "R3",
  "severity": "design",
  "claim": "None of the new macro families \u2014 wedge_setup/wedge_reject/wedge_qt/wedge_bbox_plane/wedge_bbox, scale_recip_div, project_thing, step_rows, column_params_m, column_render_params_stream, wall_setup_sgn, or the slope_div_m/point_to_angle_m/wall_x_range_m twins \u2014 has a kernel-level oracle-vs-fj test in tests/fj/test_projection_kernels.py, breaking that file's own convention (every pre-existing proj.* macro has one, and point_on_side_leaf's new sign-magnitude signature WAS re-tested there).",
  "scenario": "An edge branch no gate viewpoint provably exercises \u2014 scale_recip_div's fs11 shift arm (needs den >= 16^7), wedge_qt's Q==0 boundary on the negated plane, project_thing's clampn feet-planted path at exactly h==VIEW_H \u2014 regresses during a future edit and no test fails, because coverage is only the end-to-end frame gates on the certified config.",
  "needs_code_change": false,
  "unit": "fj-projection",
  "verdict": "confirmed",
  "v_needs_change": false,
  "v_reason": "Verified against the working tree: all thirteen listed macro families are new on this branch (absent from main:src/fj/projection.fj) and none has a kernel-level oracle-vs-fj test \u2014 tests/fj/test_projection_kernels.py's 16 tests all cover pre-existing macros, and a repo-wide grep finds the new names only in a comment (line 326, which itself notes sgnr is \"unchecked here\") and a host-only Python test. The file's convention is exactly as claimed: signature changes to existing kernels (wall_setup affine coeffs, wall_x_range abs/sign args, slope_div recip LUT) had their tests updated, and point_on_side_leaf's new sign-magnitude signature was re-tested (lines 802-827), while the new families got nothing. The cited fragile branches are real: scale_recip_div's fs11 arm needs fsh==11 (8-significant-nibble den), wedge_qt's negated-plane Q==0 boundary rides a single hex.if0 (lines 537-541), and project_thing's clamp branch (line 1564) has an eq-arm boundary at h==viewh (minor imprecision: h==VIEW_H takes okv, clamp fires at h>viewh \u2014 doesn't weaken the claim). Coverage is only the end-to-end frame gates on the certified config, so a future edit regressing an unexercised branch would pass CI. This is a design/process (R3) coverage gap, not a defect in certified shipped behavior, so needs_code_change=false per the contract's severity rules."
 },
 {
  "file": "src/fj/projection.fj",
  "line": 1837,
  "rule": "R6",
  "severity": "design",
  "claim": "The lines-mode twins (slope_div_m vs slope_div, point_to_angle_m vs point_to_angle, wall_x_range_m vs wall_x_range) duplicate the octant constant tails, the SLOPERANGE clamp logic, and the clip sequence wholesale \u2014 three near-identical copies (plus the oracle) that must be edited in lockstep with nothing enforcing it.",
  "scenario": "A correctness fix landed in slope_div (say, adjusting the wnum>=0x1000 clamp guard) but not in slope_div_m silently diverges lines mode from the fb/raster/stream modes; only the certified mode's gates check its own side, so the un-certified mode ships the stale twin until someone renders with it.",
  "needs_code_change": false,
  "unit": "fj-projection",
  "verdict": "confirmed",
  "v_needs_change": false,
  "v_reason": "Verified against src/fj/projection.fj: the three twin pairs duplicate exactly what the claim says \u2014 the 8 octant constant tails (point_to_angle 161-191 vs point_to_angle_m 1962-1992, identical constants), the SLOPERANGE 0x800 clamp + wnum>=0x1000 guard (slope_div 54-79/87 vs slope_div_m 1868-1888/1895), and the CLIPANGLE tspan clip sequence with triplicated cclip/c2clip/cnegclip constants (wall_x_range 1004-1046 vs wall_x_range_m 2057-2097) \u2014 plus the oracle in reference_model.py as another lockstep copy. No shared sub-macro or generator enforces consistency; the only tie is a comment (\"the body is the coarseslope slope_div verbatim\"). The certification story holds: build.py:186 ships raster_mode=\"lines\", so the gates exercise only the _m twins (frame_render.fj:1876/2429) while the originals serve the ungated fb/raster/stream modes (frame_render.fj:335-1009) \u2014 a fix landed on one side ships stale on the other until someone renders with it. The pairs have already deliberately diverged (norm loop vs compare tree, read_table_packed vs sdrecip/ttang lookups, scmp 8 vs cmp 2 final column test), proving edits routinely touch one twin only. However, this is a documented, working-as-certified performance design (map-unit slices, bit-identity argued at 1823-1831 and gate-verified on the shipped side) with no present defect \u2014 a design/SSOT robustness finding, so needs_code_change=false per the contract."
 },
 {
  "file": "src/fj/projection.fj",
  "line": 507,
  "rule": "none",
  "severity": "design",
  "claim": "The wedge cull (wedge_qt :507, wedge_bbox_plane) and the whole M13-mapunits block (:1824) compute on high-4-nibble slices under the invariant that every position's low 16 bits are zero, but that invariant is enforced only implicitly by pass1's stdin parse (`hex.input_dec_int` then shl 16) \u2014 no assert or comment at the input boundary marks it load-bearing for these macros.",
  "scenario": "M14 (input+sim, the declared next milestone) adds DOOM-style movement, whose canonical positions are fractional 16.16; if the sim feeds an unquantized viewx/viewy, the 4-nibble slice subtractions drop borrows from the nonzero low halves and the wedge cull / _m angle pipeline silently misculls segs \u2014 and gates re-run on integer viewpoints would still pass, so the breakage only shows in play.",
  "needs_code_change": false,
  "unit": "fj-projection",
  "verdict": "confirmed",
  "v_needs_change": false,
  "v_reason": "Claim verified against the code: wedge_qt (src/fj/projection.fj:507-511), wedge_bbox_plane (:556), and the M13-mapunits _m pipeline (:1824-1831, incl. slope_div_m's `hex.if0 4, den` zero test) all compute on high-4-nibble slices under the low-16-bits-zero invariant, and the only enforcement is the parse-then-shl in pass1 (src/doomfj/wall_renderer.py:1086-1089, `hex.input_dec_int` + `hex.shl_hex 8, 4`), whose surrounding comments (:1090-1106) never mark the quantization as load-bearing. The borrow-drop miscull scenario under a fractional M14 viewx is technically sound and would evade integer-viewpoint gates. However, the shl structurally guarantees the invariant in all current code paths (certification intact), and the invariant is prominently documented at every consumer \u2014 so this is a producer-side documentation/robustness note for a future milestone (severity design, as filed), not a latent defect requiring a code change now."
 },
 {
  "file": "src/fj/stream_render.fj",
  "line": 1279,
  "rule": "R6",
  "severity": "style",
  "claim": "The sprite y0 bias 32768 is a bare literal duplicated in both playback walkers (`hex.sub_constant 4, y0, 32768` here and at line 1205 in sprite_runs_win) and in the record side (`hex.add_constant 8, tb8, 32768` at src/fj/frame_render.fj:1418) with no shared named constant, even though the add/sub pair must stay bit-identical for sprite rows to be correct.",
  "scenario": "A future retune of the bias (the previous one-byte +128 bias was already changed once, per the M13-15M comment) edited in frame_render.fj but missed in one of the two stream_render.fj walkers shifts every sprite run by the delta; the certification gates would catch it, but only on the certified config \u2014 a knob path exercising only the missed walker (e.g. things with the V4b second fragment off) would ship the offset silently. Hoisting it into one emitter-substituted constant matches how viewh/centery/wpxstride are already passed.",
  "needs_code_change": false,
  "unit": "fj-stream_render",
  "verdict": "confirmed",
  "v_needs_change": false,
  "v_reason": "Verified: the 32768 sprite-y0 bias is a bare literal at exactly the three cited sites (frame_render.fj:1418 encode via add_constant 8; stream_render.fj:1205 and 1279 decode via sub_constant 4) with no shared named constant anywhere \u2014 the only Python-side trace is a comment at wall_renderer.py:1889. The add/sub triple must stay bit-identical for sprite rows to decode correctly, the bias was already retuned once (from the wrapping one-byte +128, per in-file comments), and sibling constants (viewh, blkshift, wpxstride) are already passed as parameters, so the hoist suggestion matches existing practice. It is a genuine R6 single-source-of-truth style finding, but the three sites agree today and the certified sweep covers both walkers on the shipped config, so it is a maintainability note, not a current or latent defect requiring a change now."
 },
 {
  "file": "src/fj/stream_render.fj",
  "line": 1205,
  "rule": "R6",
  "severity": "design",
  "claim": "The sprite y0 bias 32768 is a bare literal with no single source of truth: it appears twice on the decode side here (lines 1205 and 1279) and independently on the encode side in frame_render.fj lines 1417-1418 (hex.add_constant 8, tb8, 32768), so the bias must be kept bit-identical across three hand-maintained sites in two files.",
  "scenario": "A future edit changes the bias at the frame_render.fj record site (e.g. widening headroom for taller sprites) without touching both decode sites in this file: every sprite row shifts by the delta, and nothing catches it until a full gate rebuild -- exactly the failure mode R6's mirror-bit-for-bit rule exists to prevent, and the same class as the one-byte +128 bias wrap this two-byte bias replaced.",
  "needs_code_change": false,
  "unit": "fj-stream_render",
  "verdict": "confirmed",
  "v_needs_change": false,
  "v_reason": "Verified against the working tree: the sprite y0 bias 32768 is a bare literal at exactly the three claimed sites \u2014 decode at src/fj/stream_render.fj:1205 (sprite_runs_win) and :1279 (sprite_runs), encode at src/fj/frame_render.fj:1417-1418 \u2014 with no named constant anywhere (config.py has no bias entry despite the emit_fj_consts SSOT mechanism existing at build.py:146). R6 explicitly requires constants to come from config.py/fj_consts.fj with no duplication, so this is a genuine R6 violation. The bias is internal fj wire format (the oracle keeps raw ints per the frame_render.fj:2277 comment), so the sites cancel today and the byte-exact certification stands \u2014 the risk is latent drift if one site is edited alone, caught only at gate rebuild. Working-as-certified design finding, so needs_code_change=false."
 },
 {
  "file": "src/fj/stream_render.fj",
  "line": 220,
  "rule": "none",
  "severity": "style",
  "claim": "In emit_column_spans the wall block's trailing 'hex.mov 2, ycur, fstart' (line 220) is a dead store: control falls straight into the floor: label which unconditionally executes the identical mov at line 222.",
  "scenario": "No functional failure -- the value is overwritten before any read; the redundant mov only wastes ops in the 0x0A spans measurement mode and invites a future editor to assume the two stores serve different paths.",
  "needs_code_change": false,
  "unit": "fj-stream_render",
  "verdict": "confirmed",
  "v_needs_change": false,
  "v_reason": "In emit_column_spans (src/fj/stream_render.fj), line 220's `hex.mov 2, ycur, fstart` falls straight through the `floor:` label (221) into the identical `hex.mov 2, ycur, fstart` at line 222, with no intervening read of ycur; line 222 exists to serve the other entry path (the line-215 `hex.if0 wlen, floor` jump when the wall run is empty), so the line-220 store is a true dead store. It is harmless to output (the wall record is emitted at 216-219 first, and both paths converge to identical state), affecting only op count in the 0x0A spans mode \u2014 a style note, not a defect needing a fix now."
 },
 {
  "file": "src/fj/plane_render.fj",
  "line": 175,
  "rule": "R5",
  "severity": "latent",
  "claim": "draw_span (and draw_span_flat at line 274) replaced the signed span-loop guard `hex.scmp 8, xx, x2` with a 2-nibble `count = x2 - x1 + 1` tested only by `hex.if0`, so the signable quantity x2-x1+1 is width-truncated instead of sign-checked.",
  "scenario": "If any caller ever passes an inverted span with x2 < x1-1 (e.g. a future emitter bug or a new call site without plane_col's structural x1<=x2 guarantee), count wraps mod 256 to up to 255 and the running write_hex_and_inc pointer writes ~2x255 nibbles past the intended span, corrupting the framebuffer and whatever follows it. Evades certification because the only current caller family (frame.render_planes_spans/plane_col) closes spans exclusively at x-1 after opening at an earlier column of the same row (and the trail closes at view_w-1 >= spanx1), so x1 <= x2 holds by construction on every certified frame.",
  "needs_code_change": false,
  "unit": "fj-planes-misc",
  "verdict": "confirmed",
  "v_needs_change": false,
  "v_reason": "Factually accurate: the diff replaced draw_span's signed per-pixel guard (hex.scmp 8, xx, x2) with an 8-bit count = x2-x1+1 (plane_render.fj:173-175, count: hex.vec 2 at :226) tested only by hex.if0 (:177), and draw_span_flat (:272-276) uses the same idiom. An inverted span with x2 < x1-1 would wrap count mod 256 (2..255) and the running write_hex_and_inc cell pointer would overrun the framebuffer; x2 = x1-1 yields count = 0 and is safe. The evasion story also checks out: the only span_leaf callers (frame_render.fj:3186-3193 trailing close with x2=view_w-1>=spanx1, and plane_col doclose at :3241-3248 where a span opened at spanx1 can only close at a strictly later column) guarantee x1<=x2 structurally. However, needs_code_change=false: the wrap path is unreachable by any INPUT to the current program (it needs a future emitter bug or new call site, i.e. a robustness/defensive note, not a latent defect), and in the shipping stream/raster configs draw_span/draw_span_flat are not even emitted (wall_renderer.py:172, :1208-1215) \u2014 the kernels only exist in the older framebuffer-mode builds. The counter idiom is a deliberate byte-exact perf optimization; if hardening is wanted, a one-per-span hex.scmp 2, x1, x2 setup guard would restore the old behavior at negligible cost."
 },
 {
  "file": "src/fj/present.fj",
  "line": 128,
  "rule": "none",
  "severity": "style",
  "claim": "The load_raster_tables (0x0C) protocol doc self-contradicts on the zlight entry width: 'packed-byte, 2 bytes/entry (wait: nibble-vec 2, 1 byte via read_table)' -- a leftover drafting note shipped in the wire-format spec; the authoritative decoder (tests/fj/stream_screen.py:165, _read_packed_bytes(zlight_addr, LIGHTLEVELS*MAXLIGHTZ)) reads exactly 1 byte per entry.",
  "scenario": "A future device implementation (or hardware port) written from this comment's leading '2 bytes/entry' claim would read 4096 slots instead of 2048 and shade every raster-mode frame with garbage zlight rows; the certified 0x09/0x0A paths never exercise 0x0C, so the gates would not catch it.",
  "needs_code_change": false,
  "unit": "fj-planes-misc",
  "verdict": "confirmed",
  "v_needs_change": false,
  "v_reason": "src/fj/present.fj:128 does contain the self-contradicting drafting note (\"packed-byte, 2 bytes/entry (wait: nibble-vec 2, 1 byte via read_table)\"), and the authoritative consumer (tests/fj/stream_screen.py:165, _read_packed_bytes(zlight_addr, LIGHTLEVELS*MAXLIGHTZ)) reads exactly 1 byte per entry \u2014 matching the actual emitted table zlight_packed from generate_zlight_packed_lut_fj (lut_generator.py:209, \"1 dw-slot/entry\", explicitly NOT the nibble-vec form). The comment additionally cites the wrong generator (generate_zlight_lut_fj, the fj-side nibble-vec variant) instead of generate_zlight_packed_lut_fj. The evade-certification story holds: 0x0C is only exercised by raster-mode tests where emitter and decoder agree, so a reimplementation written from the comment's leading \"2 bytes/entry\" would misread the table unnoticed by the gates. However, shipped emitter and decoder are mutually consistent \u2014 no behavioral defect exists today; this is a comment/spec-hygiene fix, so per the style/doc convention needs_code_change is false."
 },
 {
  "file": "src/fj/present.fj",
  "line": 128,
  "rule": "R6",
  "severity": "style",
  "claim": "The load_raster_tables wire-format doc misstates the zlight table layout \u2014 it says \"2 bytes/entry\" and then contradicts itself with a leftover mid-thought \"(wait: nibble-vec 2, 1 byte via read_table)\", while the actual table handed to the device is zlight_packed at ONE packed byte per entry (generate_zlight_packed_lut_fj; stream_screen.py:165 reads exactly LIGHTLEVELS*MAXLIGHTZ single bytes).",
  "scenario": "A future device implementation (the planned non-Python screen device) written from this protocol comment instead of the Python decoder reads 2 bytes per zlight entry, doubling the stride and shading every frame with garbage colormap rows; the comment is the only prose spec of the 0x0C format.",
  "needs_code_change": false,
  "unit": "fj-planes-misc",
  "verdict": "confirmed",
  "v_needs_change": false,
  "v_reason": "Verified: present.fj:128 does say \"2 bytes/entry\" with the leftover \"(wait: nibble-vec 2, 1 byte via read_table)\" mid-thought and cites the wrong generator (generate_zlight_lut_fj), while the table actually passed to load_raster_tables is zlight_packed from generate_zlight_packed_lut_fj (wall_renderer.py:1489/1506, 1253/1259 \u2014 one packed byte per entry, per its own docstring \"NOT the nibble-vec form\"), and the only decoder reads exactly LIGHTLEVELS*MAXLIGHTZ single bytes (stream_screen.py:165). The comment is the only prose spec of the 0x0C zlight stride and would mislead a future device implementation. Doc-only: emitter and decoder agree, certification unaffected, so style severity with no code change required (fixing the comment is still worthwhile)."
 },
 {
  "file": "src/fj/plane_render.fj",
  "line": 109,
  "rule": "none",
  "severity": "style",
  "claim": "The draw_span header comment still says the shared LUTs include \"xtoviewangle (8nib read_tables)\", but line 132 now reads xtoviewangle via `hex.read_table_packed 4` against the packed 4-byte-per-entry table (generate_xtoviewangle_lut_fj) \u2014 the comment describes the retired format.",
  "scenario": "Someone regenerating or adding a reader for xtoviewangle follows the header's \"8nib read_table\" contract and emits/reads the nibble-vec layout, which silently mismatches the packed table every other consumer (projection.fj:1184/1340/1343) expects.",
  "needs_code_change": false,
  "unit": "fj-planes-misc",
  "verdict": "confirmed",
  "v_needs_change": false,
  "v_reason": "Verified: plane_render.fj:108-109 lists xtoviewangle among the \"8nib read_tables\" LUTs, but the table is emitted packed 4-bytes/entry (lut_generator.py:166-170 generate_xtoviewangle_lut_fj, wired at wall_renderer.py:470) and every consumer (plane_render.fj:132, projection.fj:1184/1340/1343) reads it via read_table_packed 4 \u2014 the comment describes the retired pre-lutpack format (yslope/distscale in the same sentence remain genuinely 8-nib read_table, so only the xtoviewangle mention is stale). It is a comment-only drift: code, generator, and all consumers agree, the generator docstring documents the packed format correctly, and any wrong-width reader would fail the byte-exact gates immediately rather than silently \u2014 style note, no code change needed now."
 },
 {
  "file": "src/fj/fixed_point.fj",
  "line": 155,
  "rule": "none",
  "severity": "style",
  "claim": "The new read_table_packed def was inserted directly under read_table's documentation block (lines 143-154: the time/space complexity formulas and the hex[:n] entry-layout contract), so those docs now visually attach to read_table_packed \u2014 whose layout is different (one dw-slot per BYTE, not per nibble) \u2014 and read_table at line 173 is left with no doc at all.",
  "scenario": "A reader costs read_table_packed using the complexity formulas at lines 143-144 (which describe read_table's per-nibble read_hex path) or builds a table to read_table's contract for a packed reader; both mislead exactly because the wrong macro sits under the doc.",
  "needs_code_change": false,
  "unit": "fj-planes-misc",
  "verdict": "confirmed",
  "v_needs_change": false,
  "v_reason": "Verified against the branch diff: on main the doc block now at fixed_point.fj:143-154 (complexity formulas and the hex[:n] / table+k*n*dw entry-layout contract) sat directly above `def read_table`; the branch inserted the M13-lutpack comment (155-159) and `def read_table_packed` (160-171) between them with no separating blank line, so the contiguous comment block now attaches to read_table_packed while read_table at line 173 has no doc. The formulas (n-parameterized, read_hex per-nibble path) and the n*dw-stride hex-entry contract genuinely do not apply to the packed reader (nb*dw stride, one dw-slot per byte, read_byte_and_inc), so the stated misreading scenarios are real. Mitigated by the packed macro's own comment stating its layout and contrasting with \"the read_table form\" \u2014 this is a documentation-placement/style issue with zero behavioral impact, so no code change is required now (a doc-shuffle commit would be nice-to-have)."
 },
 {
  "file": "src/fj/present.fj",
  "line": 50,
  "rule": "none",
  "severity": "style",
  "claim": "The M13pS0 section header says \"These two new macros\" but the section now contains eight protocol macros (0x07, 0x09, 0x0A, 0x0B, 0x0C, 0x0D, 0x0E, 0x0F), and the claim that only stream_screen.py's 0x01/0x07 extension consumes them is stale for the later raster/proj protocols.",
  "scenario": "A maintainer auditing which device commands are additive-vs-stock trusts the \"two new macros\" framing and misses that 0x0C/0x0E hand out raw table addresses (a different risk class than the pure command bytes), auditing only the first two.",
  "needs_code_change": false,
  "unit": "fj-planes-misc",
  "verdict": "confirmed",
  "v_needs_change": false,
  "v_reason": "The M13pS0 header at src/fj/present.fj:47-52 is genuinely stale: it says \"These two new macros\" but the section now holds nine macros (0x01-extended init plus command bytes 0x07/0x09/0x0A/0x0B/0x0C/0x0D/0x0E/0x0F), and its claim that only the M13pS2 stream-composite renderer consumes them while build_doom keeps the stock 8-byte init_screen is now false \u2014 build.py's shipping default raster_mode=\"lines\" makes wall_renderer.py:1272 emit present.init_screen_stream 0 and begin_frame_collines (0x0B). The 0x0C/0x0E raw-address-handoff audit scenario is also accurate. However, every later macro carries its own current doc comment naming its stream_screen.py decoder, and the rot is confined to a comment with zero effect on emitted bytes or certified behavior \u2014 a documentation/style fix, not a code defect, so needs_code_change=false per the review contract."
 },
 {
  "file": "src/doomfj/reference_model.py",
  "line": 1410,
  "rule": "none",
  "severity": "design",
  "needs_code_change": false,
  "claim": "The degrade=True defaulting uses 'or', so the adjacent comment 'any explicit value wins' is false for the value 0: an explicit deg_sliver=0 / deg_stack_scale=0 / deg_lip_scale=0 / deg_mark=0 passed together with degrade=True is silently replaced by the certified default instead of disabling that knob.",
  "scenario": "A research sweep calling render_wall_frame(..., degrade=True, deg_stack_scale=0) to measure the package with the stack far-gate OFF actually measures DEG_STACK_SCALE=32768 \u2014 the same silent knob-not-reaching-the-code failure class as the R49 SPRITE_RUN_CAP import-binding trap already documented in this file; conclusions drawn from such an ablation are wrong with no error raised. Use None sentinels ('x if x is not None else DEFAULT') or fix the comment.",
  "unit": "py-reference_model",
  "verdict": "confirmed",
  "v_needs_change": false,
  "v_reason": "Confirmed as written: reference_model.py:1407-1413 uses `x or DEFAULT` defaulting under degrade=True, so an explicit 0 \u2014 which is the documented off-value for these same knobs at lines 1616/1682/1684/1814 \u2014 is silently replaced by the certified default, and the line-1406 comment \"any explicit value wins\" is false for 0. A `degrade=True, deg_stack_scale=0` ablation would silently measure DEG_STACK_SCALE=32768 (the R49 knob-trap class). However it is design-tier, not a defect needing a code change now: the certified/shipped path (plain degrade=True) is unaffected, and every existing ablation caller (scratchpad/deg_isolate.py:27-33, scratchpad/trial25.py:88-95) passes explicit knobs WITHOUT degrade=True, so no caller in the tree can hit the trap. Recommended remedy is a comment correction (\"any explicit NONZERO value wins; to disable a knob, omit degrade=True\") or None sentinels \u2014 a documentation/cleanliness fix, matching the reviewer's own severity=design."
 },
 {
  "file": "src/doomfj/reference_model.py",
  "line": 1634,
  "rule": "none",
  "severity": "style",
  "needs_code_change": false,
  "claim": "has_up and has_lo (lines 1634-1635) are dead assignments \u2014 the face-placement conditions below test um_/lm_ directly, so these are leftovers from the pre-V5-DROP refactor.",
  "scenario": "No functional effect; a reader auditing the V5 stop/placement logic wastes time tracing two variables that feed nothing, and a future edit could 'fix' a condition by using them while the live code reads um_/lm_ == 1 inline.",
  "unit": "py-reference_model",
  "verdict": "confirmed",
  "v_needs_change": false,
  "v_reason": "Grep across src/doomfj/reference_model.py confirms has_up/has_lo exist only as assignments at lines 1634-1635 and are never read; all face-placement conditions (lines 1636, 1668-1670, 1685, 1689, 1698, 1708, 1711, 1716, 1721) test um_/lm_ inline. Dead assignments with zero functional effect \u2014 a style/readability leftover, consistent with the byte-exact certification, not a defect requiring a fix now."
 },
 {
  "file": "src/doomfj/reference_model.py",
  "line": 1149,
  "rule": "none",
  "severity": "style",
  "needs_code_change": false,
  "claim": "project_thing's docstring still documents a 5-tuple return '(x1, x2, ytop, h, istep)' but the function has returned a 6-tuple ending in tz since SPR-NEAR added pr_tz (line 1199).",
  "scenario": "A new caller written against the docstring unpacks 5 values and gets a ValueError (or silently drops tz, losing the SPR-NEAR far/near classification); the render loop already unpacks 6, so the stale doc is the only remaining 5-tuple description.",
  "unit": "py-reference_model",
  "verdict": "confirmed",
  "v_needs_change": false,
  "v_reason": "Verified against the working tree: the docstring at src/doomfj/reference_model.py:1149-1153 documents \"Returns `(x1, x2, ytop, h, istep)`\" and describes exactly those five components, while the return at line 1199 is `return x1, x2, ytop_r, h, istep, tz` \u2014 a 6-tuple. The sole Python caller (line 1540) unpacks six values (`tx1, tx2, ytop, th_px, istep, pr_tz`) and uses pr_tz for the SPR-NEAR far/near classification at line 1542; no test calls project_thing directly. So the claim is accurate and the failure scenario (a new caller written to the docstring gets a ValueError on 5-way unpack) is real. However this is documentation-only: no shipped or latent runtime behavior is wrong, certification is unaffected, and the reviewer themselves rated it style \u2014 so it does not warrant a code change now (a one-line docstring touch-up at most, rule \"none\")."
 },
 {
  "file": "src/doomfj/reference_model.py",
  "line": 1149,
  "rule": "none",
  "severity": "style",
  "claim": "project_thing's docstring documents a 5-tuple return `(x1, x2, ytop, h, istep)` but the function returns a 6-tuple ending in tz (line 1199), which the render loop unpacks as pr_tz for the SPR-NEAR distance gate.",
  "scenario": "A future caller (or the fj emitter author mirroring this SSOT) codes against the documented 5-tuple, unpacks `x1, x2, ytop, h, istep = model.project_thing(...)` and gets `ValueError: too many values to unpack` \u2014 or worse, indexes [4] expecting the last element and reads istep where the SPR-NEAR logic needs tz.",
  "needs_code_change": false,
  "unit": "py-reference_model",
  "verdict": "confirmed",
  "v_needs_change": false,
  "v_reason": "Verified: the docstring at src/doomfj/reference_model.py:1149 documents a 5-tuple `(x1, x2, ytop, h, istep)` but line 1199 returns 6 values ending in tz, which the render loop unpacks at line 1540 (`tx1, tx2, ytop, th_px, istep, pr_tz = pr`) and uses at line 1542 for the SPR-NEAR distance gate. The stale docstring is a real documentation drift on the SSOT the fj emitter mirrors, and the unpack-mismatch scenario is plausible for future callers \u2014 but it is a style/doc note, not a behavior defect (all current consumers unpack 6 correctly, certification unaffected), so no code change is required now."
 },
 {
  "file": "src/doomfj/reference_model.py",
  "line": 2137,
  "rule": "R6",
  "severity": "design",
  "claim": "_render_plane_regions_flat.band_colour duplicates the FT1 band-colour recipe verbatim \u2014 the diagonal-texel expression `tx[((ordinal & 63) * 64 + (ordinal & 63)) % len(tx)]` with a hardcoded flat width 64, plus the CENTERY half-slicing (lines 2150-2151) \u2014 instead of calling the shared `_flat_row_colours` (same expression at line 2231), so the recipe now has two sources of truth.",
  "scenario": "The shipped FT1 recipe is retuned in _flat_row_colours (e.g. the diagonal stride or the ordinal mask changes, or a 128x128 flat forces the 64 literal to become a parameter); the rung-3b `render_frame_2s` path keeps the old copy, its regions silently stop being 'bit-identical to the corresponding slice of the old whole-column fill' as the docstring at line 2126 promises, and any fj mirror baked from either copy drifts from the other. No gate catches it because rung 3b is opt-in and not in the certified sweep.",
  "needs_code_change": false,
  "unit": "py-reference_model",
  "verdict": "confirmed",
  "v_needs_change": false,
  "v_reason": "Confirmed R6 design finding. The local band_colour in _render_plane_regions_flat (reference_model.py:2132-2137, half-slicing 2150-2151) duplicates the FT1 recipe of _flat_row_colours (2226-2231) verbatim in effect \u2014 same diagonal-texel expression with hardcoded 64, same CENTERY ordinal restart \u2014 and both copies are introduced by this branch diff. The function already computes the exact ph argument _flat_row_colours takes (line 2145, matching callers at 1918-1920 and 2270-2278), so sharing is trivial and byte-identical. No gate ties the copies together: _render_plane_regions_flat is only reached via the opt-in render_frame_2s (rung 3b), whose tests compare fj against render_frame_2s itself, while the certified sweep exercises only the _flat_row_colours path \u2014 so a retune of the shared recipe would silently strand the copy and break the line-2126 'bit-identical slice' docstring promise. Current outputs are identical, so this is a maintainability/SSOT hazard, not a behavior defect; per the contract a design-severity note gets needs_code_change=false."
 },
 {
  "file": "src/doomfj/reference_model.py",
  "line": 1951,
  "rule": "none",
  "severity": "design",
  "claim": "The sprite splice and the `things_out` export live inside the `if floor_mode_ft1:` branch (lines 1951-1970), while the whole V4 thing pre-pass (projection, budgets, per-column fragment recording) runs unconditionally when `things=True` \u2014 so `things=True` without `floor_mode_ft1=True` pays the full projection cost, composites zero sprites, and returns an empty `things_out`, with no assert or warning.",
  "scenario": "A test or research harness calls render_wall_frame(things=True, sprite_wad=wad, floor_texturing=True) (the textured-floor tier) expecting sprites; the frame renders sprite-free and things_out stays empty, and the caller concludes no things were visible rather than that the mode combination is unsupported \u2014 the existing `assert sprite_wad is not None` at line ~1476 guards the wrong precondition. Not a shipped-behavior bug: the certified config always sets floor_mode_ft1.",
  "needs_code_change": false,
  "unit": "py-reference_model",
  "verdict": "confirmed",
  "v_needs_change": false,
  "v_reason": "Verified in src/doomfj/reference_model.py: the V4 thing pre-pass (subsector bucketing at 1475-1484, projection/budgets/fragment recording at 1500-1580) is gated only on `things`, while the sprite splice (1954-1970) and `things_out` export (1951-1953) sit inside `if floor_mode_ft1:` (1867); the `elif floor_texturing:` branch (1971) never composites sprites or fills things_out. So `things=True` with the default textured-floor tier pays full projection cost, renders sprite-free, and leaves things_out empty with no guard \u2014 the assert at 1476 only checks sprite_wad presence. However, every in-repo caller (tests/fj/test_visual_features.py, build.py's FT1 emit path) pairs things=True with FT1, so no shipped/certified behavior is affected; this is a design/robustness footgun for future harness calls, not a latent defect requiring a fix now."
 },
 {
  "file": "src/doomfj/wall_renderer.py",
  "line": 1026,
  "rule": "R6",
  "severity": "design",
  "claim": "Dead code that has already drifted from its live twin: subsector_action returns unconditionally at line 949 for lines mode, so the `if lines:` block at 1018-1035 (and the lines vzbank setup at 962-965) is unreachable -- and line 1026 bakes `seg_fvpidx` with only the data-bank `N*130*dw` form, missing the `ascode` arm that both `seg_cvpidx` one line above (1024) and the live bake site (lines 930-933) carry.",
  "scenario": "Unreachable today, so no shipped-frame impact. But the block looks maintained (it was half-updated for W1R via seg_lit2/seg_w1rf at 1009-1012), so a future refactor that removes the early return or reuses this path would, in an ascode build, aim every floor half-list at byte offset N*130*dw into a vpbank that does not exist while the bands-as-code walker expects list id N*2 -- every floor painted from garbage. Delete the dead lines branches (1018-1026, 1034-1035, 962-965) or route them through the one live implementation.",
  "needs_code_change": false,
  "unit": "py-wall_renderer",
  "verdict": "confirmed",
  "v_needs_change": false,
  "v_reason": "Verified in src/doomfj/wall_renderer.py: subsector_action's `if lines:` block (line 695) ends with an unconditional `return out` at line 949, so the post-949 code runs only when lines is falsy \u2014 making the `if lines:` branches at 1018-1026 and 1034-1035 and the lines arm of the vzbank setup at 962-965 unreachable in every configuration (`lines` is set once at line 214 and never reassigned). The drift is real: line 1026 bakes seg_fvpidx with only the data-bank `{id*130}*dw` form, while seg_cvpidx at 1024-1025 and the live bake site at 930-933 both carry the `id*2 if ascode` arm; an ascode revival of this path would aim floor half-lists at nonexistent vpbank offsets while the as-code walker (vzcbase, line 666) expects stride-2 list ids. However, since the branch cannot execute under any current knob, this is dead-code hygiene (severity=design as filed), not a latent defect reachable by any input \u2014 so no code change is required now, though deleting the dead branches is reasonable preventive cleanup."
 },
 {
  "file": "src/doomfj/wall_renderer.py",
  "line": 1577,
  "rule": "R6",
  "severity": "design",
  "claim": "_lines_bake_bank re-implements, line for line, the zidx/zlight/colormap pair-merge walk of _band_pair_lists (line 1514) instead of calling it, even though _band_pair_lists' docstring names itself 'the SSOT pair-list producer both band representations bake from'; _lines_sky_bank's half() (line ~2050) likewise duplicates _sky_pair_lists.half() (line 1560).",
  "scenario": "The shipped ascode tier and the two_sided/ablate data-bank tier bake their pixels through two separately maintained copies of the same 15-line loop. An edit to one copy -- e.g. changing the FT1 diagonal-strip rule at line 1531 but not 1606, or the run-merge condition -- silently forks the frames the ablation baselines and the two_sided lab render against the shipped output, and no test compares the two representations. Have _lines_bake_bank consume _band_pair_lists' output (it only adds the [n][pairs][pad] cell packing and the MAX_BANDS//2 assert).",
  "needs_code_change": false,
  "unit": "py-wall_renderer",
  "verdict": "confirmed",
  "v_needs_change": false,
  "v_reason": "Confirmed against the working tree. (1) The main-bank duplication is real and line-for-line: _band_pair_lists (src/doomfj/wall_renderer.py:1514-1547) and the inner loop of _lines_bake_bank (:1593-1620) contain the identical 15-line walk \u2014 same vzs/ph/lvl derivation, same FT1 diagonal-strip rule (tx[(i*64+i)%len(tx)] at :1531 vs :1606), same ordinal-advance and same run-merge condition (pairs[-1][1]==colr), iterated in the identical class-major/key/asc-desc order. _lines_bake_bank adds only the [n][pairs] cell flattening, the LINES_HALF_SLOTS padding, and the MAX_BANDS//2 assert (:1621-1626), so it could trivially consume _band_pair_lists' output as the reviewer says. (2) The sky duplication is also real: _sky_pair_lists.half (:1560-1569) and _lines_sky_bank.half (:2037-2054, not ~2050 exactly \u2014 minor line slip) both re-walk rm.sky_texel_u with the same change-detect/extend-last-run merge, differing only in packing. (3) Git shows _band_pair_lists was introduced on this branch (429e15a, bands-as-code) by copying the older _lines_bake_bank loop rather than refactoring it, and its docstring's claim to be 'the SSOT pair-list producer both band representations bake from' is false \u2014 only the ascode path (:604) calls it; the data-bank tier (:1459) and two_sided/ablate builds bake through the separate copy. (4) No test references either function (grep of tests/ finds nothing), and the byte-exact certification gates run the shipped ascode config, so a future edit to one copy (e.g. the FT1 strip rule or the merge condition) would silently fork the ablation/two_sided baselines from shipped frames without any gate firing \u2014 exactly the reviewer's scenario. This is an R6/SSOT design violation and a genuine latent-divergence hazard, but both copies are provably identical today (each tier was certified byte-exact against the oracle in its era), so there is no current or latent wrong-output defect in shipped or lab behavior \u2014 it is a maintainability refactor, hence needs_code_change=false per the design-note convention. The suggested fix (have _lines_bake_bank and _lines_sky_bank consume the pair-list producers) is sound and order-compatible."
 },
 {
  "file": "src/doomfj/wall_renderer.py",
  "line": 1026,
  "rule": "R6",
  "severity": "design",
  "claim": "The whole lines-mode arm of the fall-through half of subsector_action is unreachable dead code (the `if lines:` branch returns unconditionally at line 949, so `or lines` at 1004 and the `if lines:` blocks at 1018-1026 and 1034-1035 can never run) -- and the dead copy is internally wrong: `seg_fvpidx` at line 1026 always bakes the data-bank form `{...*130}*dw` and lacks the `ascode` branch that its own `seg_cvpidx` twin (line 1024) and the live copy (lines 930-933) both have.",
  "scenario": "A maintainer unifying the two subsector_action halves (or removing the early return) re-activates this path and, with the shipped ascode=1 config, every one-sided seg's floor visplane index becomes a huge dw byte-offset where the bands-as-code walker expects a small list id -- every floor in the frame renders from garbage. Today it is dead weight that contradicts the live code, which is exactly the drift R6 exists to prevent.",
  "needs_code_change": false,
  "unit": "py-wall_renderer",
  "verdict": "confirmed",
  "v_needs_change": false,
  "v_reason": "Verified in src/doomfj/wall_renderer.py: the lines-mode branch of subsector_action (opened at line 695) returns unconditionally at line 949, and `lines` (bound once at line 214, never reassigned) cannot be true past that point \u2014 so `or lines` at 1004 and the `if lines:` blocks at 1018-1026 and 1034-1035 are dead under every config. The dead copy is also internally inconsistent exactly as claimed: seg_cvpidx (1024-1025) branches on ascode but seg_fvpidx (1026) always bakes the data-bank `*130}*dw` form, whereas the live copy (930-933) branches on ascode for both fields; the shipped certified lines config runs ascode=1 (line 601) through the live copy only. Certification is unaffected because the code never executes, which is why this is severity=design R6 drift, not a shipped bug \u2014 hence needs_code_change=false (dead-code cleanup, no real or latent runtime defect)."
 },
 {
  "file": "src/doomfj/wall_renderer.py",
  "line": 1515,
  "rule": "R6",
  "severity": "design",
  "claim": "`_band_pair_lists`'s docstring calls it \"The SSOT pair-list producer both band representations bake from\", but `_lines_bake_bank` (lines 1593-1620) does not call it -- it re-implements the identical zidx-walk/zlight/colour-merge loop verbatim, so the shipped bands-as-code representation and the data-bank representation (used by every ablate measurement build and the two_sided lab) bake from two separate copies that must stay bit-identical by hand.",
  "scenario": "A future band tweak (e.g. an FT1 strip change) lands in one copy only: ascode (shipped) and data-bank (ablate) builds silently render different pixels, so every ops-by-delta ablation measurement from then on prices a renderer that is not the shipped one -- the same class of silent divergence the memory file already flags for the no-stack ablation config. Fix is mechanical: have _lines_bake_bank consume _band_pair_lists' output and keep only the [n][pairs] packing and MAX_BANDS assert.",
  "needs_code_change": false,
  "unit": "py-wall_renderer",
  "verdict": "confirmed",
  "v_needs_change": false,
  "v_reason": "All specifics verified in src/doomfj/wall_renderer.py: the line-1515 docstring calls _band_pair_lists \"The SSOT pair-list producer both band representations bake from\", but _lines_bake_bank (1577-1627) never calls it \u2014 lines 1593-1620 are a token-for-token copy of the 1522-1546 zidx-walk/zlight/FT1-strip/colour-merge loop, differing only in the [n][pairs] packing, MAX_BANDS assert, and slot padding. The consumers split exactly as claimed: ascode (line 601, shipped: lines and not two_sided and not ablate) bakes from _band_pair_lists at line 604, while ablate/two_sided builds bake the data bank from _lines_bake_bank at line 1459 \u2014 so shipped and measurement builds depend on two hand-synchronized copies, and a one-sided future edit would silently make ablation measurements price a non-shipped renderer (the same divergence class the memory file flags for the no-stack ablation config). Confirmed as an R6 SSOT violation with a wrong docstring; needs_code_change=false because the copies are currently bit-identical, the shipped path is certified, and no build misrenders today \u2014 this is a design/robustness finding (the mechanical consume-the-helper refactor is worthwhile but not a present defect)."
 },
 {
  "file": "src/doomfj/wall_renderer.py",
  "line": 2018,
  "rule": "none",
  "severity": "style",
  "claim": "`_lines_sky_bank`'s warning docstring states the bank holds 2*tw lists and that \"both addends are already in [0, tw-1], so their sum never exceeds 2*tw-2\", but the code bakes SKY_BANK_MUL*tw = 3*tw lists precisely because fj's unmasked skybase is a whole byte (0..255, reaching 255+127=382), the very bug commit 2086280 fixed; `_sky_pair_lists`' docstring (line 1552, \"2*tw entries\") repeats the stale claim, and frame_render.fj:2006 (\"the bank holds 256\") is stale the same way.",
  "scenario": "A size-conscious maintainer trusts the documented 2*tw-2 bound, shrinks SKY_BANK_MUL back to 2 to save ~477k baked characters, and reintroduces the out-of-range bank read for view angles whose unmasked skybase exceeds tw-1 -- sky columns desynchronise exactly as in the pre-2086280 bug. The comment documents the oracle's masked invariant, not the fj invariant that actually sizes the bank.",
  "needs_code_change": false,
  "unit": "py-wall_renderer",
  "verdict": "confirmed",
  "v_needs_change": false,
  "v_reason": "Verified in the working tree: SKY_BANK_MUL=3 (wall_renderer.py:2003) and both bake loops emit 3*tw=384 lists, while the _lines_sky_bank docstring (wall_renderer.py:2018-2019, plus the masked-skybase formula at 2015), the _sky_pair_lists docstring (1551-1552), and src/fj/frame_render.fj:2006-2007 (\"bank holds 256 lists ... max 253\") all still document the pre-fix 2*tw/masked invariant. Commit 2086280's message states verbatim that fj's unmasked skybase is a whole byte reaching 255+127=382 requiring 3*tw=384 lists \u2014 the commit fixed the constant but not the prose. The shipped code is correct (certification unaffected); this is stale documentation that invites re-shrinking the bank and reintroducing the fixed bug, so it is a style/doc finding, not a code defect requiring change now."
 },
 {
  "file": "src/doomfj/wall_renderer.py",
  "line": 43,
  "rule": "R8",
  "severity": "style",
  "claim": "Dead imports: `generate_coarse_bounds_fj` (line 43), `generate_zlight_cuts_fj` (line 21) and `DEG_HD_BUDGET` (line 31) are imported and never referenced anywhere in the file -- residue of the measured-and-rejected coarse-cull pre-pass and option-B HD-budget experiments.",
  "scenario": "No runtime effect; they imply to a reader that this emitter still wires the coarse cull and the HD budget (both were measured net-loss/off per the campaign notes), and any lint pass with unused-import warnings enabled (the Python analogue of the R8 werror hygiene bar) flags the file.",
  "needs_code_change": false,
  "unit": "py-wall_renderer",
  "verdict": "confirmed",
  "v_needs_change": false,
  "v_reason": "All three imports (generate_zlight_cuts_fj line 21, DEG_HD_BUDGET line 31, generate_coarse_bounds_fj line 43 of src/doomfj/wall_renderer.py) appear nowhere else in the file \u2014 grep over the full file (including generated-code string templates) shows only the import lines \u2014 and no repo consumer imports these names through wall_renderer (all callers import only emit_wall_renderer/_seg_xorby_block/_seg_xorby_use), so removal is safe. Matches the campaign record: coarse-cull was measured-and-rejected, option-B HD budget is off/net-loss. Pure R8-hygiene/style residue with zero runtime effect; not a defect, so no code change required now."
 },
 {
  "file": "src/doomfj/lut_generator.py",
  "line": 898,
  "rule": "R5",
  "severity": "latent",
  "claim": "walk_win derives the pattern tier from `hex.sub 2, wl2, ctake` (wl2 = fstart - ctake) followed by unsigned `hex.cmp` tier compares, with no guard that fstart > ctake \u2014 a difference is a signable quantity, and a wrapped negative silently classifies as the near tier.",
  "scenario": "If any future emit_region caller ever passes a degenerate piece with fstart <= ctake while wlo < whi (the only guard the macro has, line 895 \u2014 today this is impossible only because the window-within-wall invariant makes wlo<whi imply ctake<fstart), wl2 wraps to ~0xFF, the tier tree picks tier 3 (near 6px masonry), and the walker emits a wrong-pattern run list instead of trapping or emitting nothing. Evades certification because all certified call sites (stream_render.fj:857,1163) satisfy the invariant; the hazard is only reachable via a future caller.",
  "needs_code_change": false,
  "unit": "py-lut_generator",
  "verdict": "confirmed",
  "v_needs_change": false,
  "v_reason": "The mechanics are exactly as claimed: walk_win's only guard is the unsigned wlo<whi cmp (lut_generator.py:895), wl2 = fstart - ctake (line 898) wraps mod 256 on a degenerate caller, and the unsigned tier tree (lines 847-851) would classify ~0xFF as tier 3 (near). But the hazard is unreachable by construction, not by convention: both call sites clamp the window into the wall span immediately before the call \u2014 emit_region computes wlo=max(lo,ctake) / whi=min(hi,fstart) (stream_render.fj:1141-1149) and steps_face computes fwl=max(fy1,rlo) / fwh=min(fy2p,rhi) with fy1<=fy2 separately gated (stream_render.fj:839-855) \u2014 so the macro's wlo<whi gate provably implies fstart>ctake at every existing caller. Under the macro's documented contract wl2 is a length (unsigned), so hex.cmp is the R5-correct primitive; the identical unguarded pattern exists on the certified walk/face_pattern path. This is a future-caller robustness note, not a latent defect; adding an scmp/sign guard would cost ops on every patterned wall column of every frame in a 20M-budget renderer to protect an unreachable branch. No code change warranted now \u2014 at most a one-line contract comment on the macro."
 },
 {
  "file": "src/doomfj/lut_generator.py",
  "line": 193,
  "rule": "none",
  "severity": "style",
  "claim": "generate_yslope_packed_lut_fj is now an exact functional duplicate of the new generate_packed_lut_fj(label, yslope_table(...), 3) introduced in this same diff, but with divergent emission style: no header comment, decimal instead of hex byte literals, no indentation, assert instead of ValueError, and no trailing newline (every sibling generator returns text ending in \"\\n\").",
  "scenario": "Two emitters encoding the same `;byte*dw` little-endian convention can drift independently (e.g. a future endianness or validation fix lands in generate_packed_lut_fj only); the missing trailing newline also means any future caller that concatenates the string directly (rather than via a \"\\n\"-joined decls list as wall_renderer.py:1488 does today) would merge the last data op with the next label into one malformed line.",
  "needs_code_change": false,
  "unit": "py-lut_generator",
  "verdict": "confirmed",
  "v_needs_change": false,
  "v_reason": "Verified by reading src/doomfj/lut_generator.py (lines 107-120 vs 193-206) and by executing both emitters at the shipped 160x100 config: generate_yslope_packed_lut_fj produces a semantically identical byte stream to generate_packed_lut_fj(label, yslope_table(...), 3) (normalized comparison equal), differing only in the claimed style points \u2014 no header comment, decimal instead of hex literals, no indentation, assert instead of ValueError (which also vanishes under python -O), and no trailing newline while every sibling generator returns one. The generic function's docstring even calls itself \"the yslope_packed precedent generalized\", and six other generators were migrated to it in this same diff, leaving the precedent as the lone divergent duplicate. The trailing-newline concatenation hazard is latent, not live: all three callers (wall_renderer.py 1488/1505/1827) go through the \"\\n\".join at wall_renderer.py:1269. Certified output is unaffected (comments/whitespace/radix don't change assembled bits), so this is a valid style/duplication note, not a defect requiring a fix now."
 },
 {
  "file": "src/doomfj/lut_generator.py",
  "line": 714,
  "rule": "none",
  "severity": "style",
  "claim": "In generate_bands_walk_fj the `elif owner[k] != k` branch emits exactly the same line as the `else` branch (`;vpb_t{k}`), and its 7-line comment narrates an abandoned design (\"The body must therefore end by jumping through a RETURN REGISTER -- simplest: ... Instead: ...\") that no longer matches the code, which uses per-id stl.fcall thunks for ALL entries.",
  "scenario": "A future editor reading the comment concludes shared and unshared entries take different dispatch paths and 'optimizes' the unshared case to jump straight to the body without the fcall thunk \u2014 which would break the fret-based exit (vpb_fin{k} does stl.fret vpb_x, so a body entered without fcall jumps through a stale/zero vpb_x).",
  "needs_code_change": false,
  "unit": "py-lut_generator",
  "verdict": "confirmed",
  "v_needs_change": false,
  "v_reason": "Verified in src\\doomfj\\lut_generator.py: the `elif owner[k] != k` branch (line 714) and the `else` (line 721) both emit the identical `;vpb_t{k}` line (lines 720/722), and the thunk loop (lines 725-728) emits a per-id `stl.fcall vpb_body{owner[k]}, vpb_x` thunk for ALL entries, not just shared ones \u2014 so the comment's \"entries with shared content get their own tiny handler\" framing misdescribes the uniform path (its \"Instead:\" clause does match the mechanism, so \"abandoned design\" is a mild overstatement, but the shared-only scoping is genuinely misleading). The hazard scenario is mechanically sound: bodies exit via `stl.fret vpb_x` (line 753) with vpb_x a raw jump op init `;0` (line 754), so a body entered without the fcall thunk jumps through zero/stale vpb_x. No external consumer distinguishes the branches (vpb_t labels referenced only inside the generated block; call sites in src\\fj\\stream_render.fj just invoke vpb_walk), and the emitted fj text is identical either way, so the certified binary is unaffected. Style/comment-clarity finding only \u2014 dead branch plus misleading comment, no shipped or latent runtime defect \u2014 hence no code change required now."
 },
 {
  "file": "src/doomfj/lut_generator.py",
  "line": 769,
  "rule": "none",
  "severity": "style",
  "claim": "The generate_w1r_walls_fj docstring documents both macro signatures without the wall_lit2 parameter (`walk wlen, ctake, fstart, gnrow, wall_lit, cmidx` at line 769 and `walk_win ctake, fstart, wlo, whi, gnrow, wall_lit, cmidx` at line 776), but the generated defs (lines 873 and 892) and every fj call site (stream_render.fj:813,857,1163,1380) take wall_lit, wall_lit2 \u2014 7- and 8-argument signatures respectively.",
  "scenario": "Someone wiring a new call site from the docstring passes 6/7 arguments, shifting cmidx into the wall_lit2 slot; the fj macro expansion then either fails to assemble or silently binds the wrong registers, and the W1R-2C alt-colour runs read from cmidx garbage.",
  "needs_code_change": false,
  "unit": "py-lut_generator",
  "verdict": "confirmed",
  "v_needs_change": false,
  "v_reason": "Verified in the working tree: the generate_w1r_walls_fj docstring (src/doomfj/lut_generator.py:769 and :776) documents `walk` with 6 params and `walk_win` with 7, omitting wall_lit2, while the generated defs at lines 873 and 892 take 7 and 8 params respectively (wall_lit, wall_lit2), and all fj call sites (src/fj/stream_render.fj:813, 857, 1163, 1380) pass the wall_lit2 argument. The docstring is stale from before the W1R-2C alt-colour change (whose mechanism the inner _body docstring at lines 795-798 correctly describes). Generated code and every caller are correct and certification is unaffected, so this is a documentation-only style finding \u2014 worth a docstring touch-up but not a code defect."
 },
 {
  "file": "src/doomfj/lut_generator.py",
  "line": 221,
  "rule": "R3",
  "severity": "design",
  "claim": "generate_zlight_cuts_fj is dead code: it has no caller anywhere in the repo (wall_renderer.py:21 imports it but never calls it), its fj consumer plane_bands.fj::build_bands_bs is itself never invoked, and it has no test.",
  "scenario": "Any future edit to zlight_table or to this function silently rots \u2014 no build emits zl_nextcut/zl_prevcut and no test would catch a wrong cut table, so whoever eventually wires build_bands_bs in inherits an unverified table generator plus a docstring that hardcodes the lvl*128 stride the code does not actually enforce (len(row) is used, but a mismatched fj-side stride would misindex every level above 0).",
  "needs_code_change": false,
  "unit": "py-lut_generator",
  "verdict": "confirmed",
  "v_needs_change": false,
  "v_reason": "Verified: generate_zlight_cuts_fj (lut_generator.py:221) has zero call sites \u2014 wall_renderer.py:21 imports it but never uses it; its fj consumer build_bands_bs (plane_bands.fj:285) is defined but never invoked (wall_renderer.py:1824 wires only \"plane.build_bands 1\"), so fj macro pruning means no build ever emits zl_nextcut/zl_prevcut; and no test in tests/ references any of it. The docstring's lvl*128 stride is likewise unenforced (code uses len(row)). All accurate \u2014 but it is dead code that never reaches any binary, so it's a design/process (R3) finding, not a defect requiring a fix now."
 },
 {
  "file": "src/doomfj/lut_generator.py",
  "line": 193,
  "rule": "R6",
  "severity": "design",
  "claim": "generate_yslope_packed_lut_fj hand-rolls exactly what generate_packed_lut_fj now generalizes (its own docstring says 'the yslope_packed precedent generalized') but with three divergences: range validation via bare assert (silently stripped under python -O, unlike the ValueError every other generator raises), no header comment line, and no trailing newline on the returned string (every other generator in the file ends with '\\n').",
  "scenario": "Under python -O an out-of-range yslope value (e.g. after a future VIEW_H/f-number change pushing a slope past 2^24) is silently truncated into wrong packed bytes instead of failing the build; separately, a caller that concatenates this generator's output directly before another label without inserting a newline merges the last ';X * dw' op with the next line into an assembler error (or worse, a parse the werror build only catches late). Delegating to generate_packed_lut_fj(label, yslope_table(view_w, view_h), 3) removes all three divergences.",
  "needs_code_change": false,
  "unit": "py-lut_generator",
  "verdict": "confirmed",
  "v_needs_change": false,
  "v_reason": "All three divergences verified at lut_generator.py:193-206: bare `assert` (line 203) vs ValueError in every sibling (84/101/116), no header comment (line 201), no trailing newline (line 206); generate_packed_lut_fj (107-120) is value-identical (same yslope_table SSOT, same LE 3-byte order, same 2^24 bound) so the proposed delegation is safe. But both failure scenarios are latent double-hypotheticals: no `python -O`/PYTHONOPTIMIZE anywhere in the repo's tooling and the current max value 0xA00000 is in-bounds; all current callers (wall_renderer.py:1269 via decls at 1488/1505/1827, test_plane_bands_fj.py:32) join with \"\\n\".join so no concatenation merge can occur today. Accurate design/robustness finding, correctly severity=design \u2014 not a fix-now defect."
 },
 {
  "file": "src/doomfj/lut_generator.py",
  "line": 37,
  "rule": "none",
  "severity": "style",
  "claim": "__all__ was updated for generate_packed_lut_fj and generate_emit_dispatch_table_fj but not for the seven other new public generators (generate_slopediv_recip_lut_fj, generate_slopediv_recip8_lut_fj, generate_yslope_packed_lut_fj, generate_zlight_packed_lut_fj, generate_zlight_cuts_fj, generate_bands_walk_fj, generate_w1r_walls_fj), leaving the export list inconsistently partial.",
  "scenario": "A consumer using 'from doomfj.lut_generator import *' (or tooling relying on __all__, e.g. an API-surface test or documentation generator) silently misses the seven unlisted generators while seeing their two siblings, and star-importing code that worked for generate_packed_lut_fj fails with NameError for generate_yslope_packed_lut_fj; current callers are unaffected because wall_renderer.py imports names explicitly.",
  "needs_code_change": false,
  "unit": "py-lut_generator",
  "verdict": "confirmed",
  "v_needs_change": false,
  "v_reason": "Verified against C:\\Users\\tomhe\\Documents\\doom-flipjump\\src\\doomfj\\lut_generator.py: __all__ (lines 37-55) was extended on this branch with exactly two of the nine new public generators (\"generate_packed_lut_fj\" line 47, \"generate_emit_dispatch_table_fj\" line 44), while the other seven new module-level public functions \u2014 generate_slopediv_recip_lut_fj (def line 145), generate_slopediv_recip8_lut_fj (153), generate_yslope_packed_lut_fj (193), generate_zlight_packed_lut_fj (209), generate_zlight_cuts_fj (221), generate_bands_walk_fj (677), generate_w1r_walls_fj (765) \u2014 are absent from __all__. The git diff vs main confirms all nine are branch additions, so the export list is genuinely inconsistently partial as claimed. However, no code in the repo star-imports the module or consumes __all__ (all callers \u2014 wall_renderer.py lines 14/44, build.py line 20, texturecompiler.py line 22 \u2014 import names explicitly, and no test inspects __all__), so the failure scenario is hypothetical for the current codebase. This is a real but style-only inconsistency with zero effect on shipped/certified behavior; not a defect worth a code change now (severity=style, rule none \u2014 R6 does not cover Python export lists)."
 },
 {
  "file": "src/doomfj/nodebuilder.py",
  "line": 1,
  "rule": "R2",
  "severity": "design",
  "claim": "Both new logic modules (nodebuilder.py, 293 lines; mapsimplify.py, 370 lines) land with zero coverage in tests/ \u2014 no test references NodeBuilder, rebuild_wad, or simplify anywhere under tests/ \u2014 so the invariants their comments cite as 'measured' bug fixes (the stale-adj endpoint guard, the sagitta drift ledger, the original-linedef partition rule, the collinear-seg routing convention that must mirror _point_side) have no regression net.",
  "scenario": "The shipped E1M1-lite WAD is validated transitively by the renderer's byte-exact certification, but the tools themselves are re-run artifacts: anyone re-running simplify with different tolerances or re-noding another map gets output whose correctness rests on scratchpad-only validators \u2014 e.g. reintroducing the decimation endpoint-move bug (documented at mapsimplify.py:582-585 as having deleted a real 32-unit wall) would go undetected until a visual campaign notices an unclaimed black column. A small round-trip test (build a toy map, rebuild_wad, assert bake_bsp point-location matches a ray oracle at sampled points; assert simplify preserves protected sectors and step-climbability) would lock the conventions in.",
  "needs_code_change": false,
  "unit": "py-map-tools",
  "verdict": "confirmed",
  "v_needs_change": false,
  "v_reason": "Verified: no file under tests/ references NodeBuilder, rebuild_wad, mapsimplify, or simplify (grep across tests/fixtures, tests/fj, tests/host returns zero matches), while both modules are new 293/370-line logic additions on this branch. The invariants the claim lists are real comment-documented measured bugs with no regression net: the stale-adj endpoint guard (mapsimplify.py:236-241, not 582-585 as the claim mis-cites \u2014 the file is 370 lines), the sagitta drift ledger (mapsimplify.py:217-219, 266-277), the original-linedef partition rule (nodebuilder.py:49-53), and the _point_side/collinear routing convention (nodebuilder.py:7-13, 85). The only validators are scratchpad/make_e1m1_lite.py + nb_validate, which are UNTRACKED (?? in git status), so a fresh clone has no net at all. The shipped WAD is transitively certified, so this is an R2 design/process finding, not a shipped defect \u2014 needs_code_change=false per the contract (design notes get false)."
 },
 {
  "file": "src/doomfj/nodebuilder.py",
  "line": 1,
  "rule": "R2",
  "severity": "design",
  "claim": "A new 293-line logic module (BSP partitioning, seg classification/splitting, bbox computation, WAD lump serialization) ships with zero tests under tests/ \u2014 the only exercisers are scratchpad scripts (scratchpad/make_e1m1_lite.py), which are untracked working files.",
  "scenario": "A future edit to _classify's eps handling or _split's offset accumulation (both carry measured-bug commentary: 'point_in_subsector landed (-309,-44) in the wrong sector') regresses silently; nothing in the test suite rebuilds even a trivial map and checks leaf convexity, child-ref validity, or round-trip through bake_bsp, so the regression is only caught by a full visual campaign.",
  "needs_code_change": false,
  "unit": "py-map-tools",
  "verdict": "confirmed",
  "v_needs_change": false,
  "v_reason": "Core claim verified: src/doomfj/nodebuilder.py is 293 lines of real BSP/serialization logic (branch commits 53f3be6, 1c79637) with zero references from tests/ (grep for nodebuilder/NodeBuilder/rebuild_wad/mapsimplify finds nothing), and even the committed tests/fixtures/e1m1_lite.wad output is loaded by no test \u2014 so an edit to _classify's eps band (lines 84-95) or _split's offset accumulation (line 105) would regress silently until a manual gate/visual campaign, exactly the R2 code-visible violation (new logic module, no test). The measured-bug commentary the claim cites is verbatim at lines 50-53. One detail is wrong but non-substantive: scratchpad/make_e1m1_lite.py is tracked (git ls-files confirms), not untracked. As a process/design finding (host-side tooling, correctness of the one shipped output already validated by the six byte-exact gates), needs_code_change=false per the unit contract."
 },
 {
  "file": "src/doomfj/mapsimplify.py",
  "line": 1,
  "rule": "R2",
  "severity": "design",
  "claim": "A new 370-line logic module ships with zero tests under tests/, despite its own comments documenting three subtle measured regressions (duplicate-vertex T-junction sliver, stale-adjacency wall deletion, sagitta drift) whose guards are exactly the kind of invariants a unit test should pin.",
  "scenario": "Someone 'simplifies' the _decimate guard `if v not in (a.v1, a.v2)...` (line 241) or the duplicate-vertex canonicalization (line 105) \u2014 both look redundant without their war-story comments \u2014 and the next e1m1-lite bake silently deletes a wall span or opens an unclaimed black column, rediscovered only by eyeballing renders.",
  "needs_code_change": false,
  "unit": "py-map-tools",
  "verdict": "confirmed",
  "v_needs_change": false,
  "v_reason": "src/doomfj/mapsimplify.py (371 lines, new on this branch) has zero test coverage \u2014 no file under tests/ references it \u2014 while its own comments at lines 96-104, 236-241, and 266-277 document three measured regressions (duplicate-vertex T-junction sliver, stale-adjacency wall deletion, sagitta drift) whose guards look redundant without the comments and are exactly the invariants a unit test should pin. The byte-exact gate certification cannot catch a regression here: it compares fj vs the Python oracle on the same baked map, so a simplifier bug corrupts both sides identically and the gates still pass \u2014 the failure mode is silent map damage found only by eyeballing renders, as the comments themselves attest happened. Valid R2/design finding; not a defect in the code as written, so no code change required now."
 },
 {
  "file": "src/doomfj/mapsimplify.py",
  "line": 251,
  "rule": "none",
  "severity": "style",
  "claim": "In _decimate, `flipped` (reversed sector-pair) admits line pairs past the pair-compatibility filter, but both merge branches additionally require `same`, so a flipped-only pair can never merge \u2014 the variable is a dead condition that reads as if reversed-orientation chains are handled.",
  "scenario": "Two chained lines whose front/back sectors are swapped (pa == (pb[1], pb[0]) but pa != pb) pass the `if not (same or flipped): continue` filter, then fail both `a_fwd and b_fwd and same` and the reversed-continuation `... and same` check and fall out unmerged; a maintainer extending decimation trusts the filter's apparent flipped support and wonders why flipped chains never decimate (or 'fixes' a merge branch to honor flipped without the sidedef-orientation swap that case actually needs).",
  "needs_code_change": false,
  "unit": "py-map-tools",
  "verdict": "confirmed",
  "v_needs_change": false,
  "v_reason": "Verified in src/doomfj/mapsimplify.py: `flipped` (line 251) lets reversed-sector-pair line pairs past the line-252 filter, but both merge branches (line 282 forward case, line 284 reversed-continuation case) additionally require `same`, and no state is mutated between the filter and those branches \u2014 so a flipped-only pair always falls through to the `continue` at line 289 with zero effect, behaviorally identical to being filtered out. The condition is dead as claimed; it misleadingly reads as flipped-chain support, and a naive future 'fix' would need the sidedef front/back swap the code lacks. No behavioral defect (consistent with byte-exact certification), so style-only: needs_code_change=false."
 },
 {
  "file": "src/doomfj/coarse_cull.py",
  "line": 1,
  "rule": "R3",
  "severity": "design",
  "claim": "coarse_cull.py is a new logic module (octant fold, wedge thresholds, interval arithmetic, an fj table emitter) with zero tests anywhere in tests/, and its only non-scratchpad consumer is a dead import (wall_renderer.py:43 imports generate_coarse_bounds_fj and never calls it).",
  "scenario": "Nothing exercises the superset property (e.g. octant_wedge_idx + coarse_angle_bounds vs point_to_angle over random deltas) or the fj-table byte layout, so a regression in _octant_map or the short-arc pick at line 54 would go unnoticed until the cull is wired into the renderer, where it would manifest as vanishing walls with no failing test pointing at this file.",
  "needs_code_change": false,
  "unit": "py-infra",
  "verdict": "confirmed",
  "v_needs_change": false,
  "v_reason": "All three sub-claims verified against the working tree: (1) src/doomfj/coarse_cull.py is a new logic module (octant fold, wedge thresholds, interval short-arc pick at line 54, fj table emitter) with zero references anywhere in tests/ (grep 'coarse' in tests/ = no matches; the octant/wedge test hits are point_to_angle tests, unrelated); the only harness exercising it is scratchpad/test_coarse_bracket_fj.py, outside the suite. (2) wall_renderer.py:43 imports generate_coarse_bounds_fj and never uses it \u2014 the only coarse_cull reference in the file. (3) The fj macro proj.coarse_bracket (projection.fj:217) and its coarse_bounds table are likewise reachable only from scratchpad, so the certification gates never execute this code and a regression would surface only at future wire-in. However, wall_renderer.py:519's comment shows the coarse-cull pre-pass was priced (+24.7M) and deliberately left unwired, making this dormant-by-decision code plus a stale import \u2014 a valid R3 design/hygiene finding, not a real or latent defect in certified behavior, so needs_code_change=false per the contract's design-note rule."
 },
 {
  "file": "src/doomfj/coarse_cull.py",
  "line": 1,
  "rule": "R2",
  "severity": "design",
  "claim": "The whole coarse-cull path is dead code shipped into src/: generate_coarse_bounds_fj is imported by wall_renderer.py:43 but never called, proj.coarse_bracket (src/fj/projection.fj:217) has no call site, reference_model.py never consumes octant_wedge_idx/coarse_angle_bounds despite the module docstring's 'shared (R6 SSOT) by ... the host reference' claim, and the new logic file has no test \u2014 the feature was priced at +24.7M and rejected (wall_renderer.py:519 R23/R32 note).",
  "scenario": "A future session trusts the docstring's SSOT/superset claims and wires coarse_bracket into a build without re-derivation: the never-tested table meets the host/fj degenerate divergence (octant_wedge_idx returns None for (0,0); coarse_bracket computes octant 0/wedge 7 and reads a bogus ~ANG45 interval), and there is no test to catch guard-adequacy or index-layout drift. Either delete the src copies (keep the scratchpad prototypes) or add a consumer-plus-test before the docstring's R6 claim is true.",
  "needs_code_change": false,
  "unit": "py-infra",
  "verdict": "confirmed",
  "v_needs_change": false,
  "v_reason": "Every element verified: generate_coarse_bounds_fj is imported at src/doomfj/wall_renderer.py:43 with zero call sites in src/ or the test suite (only scratchpad/test_coarse_bracket_fj.py uses it); proj.coarse_bracket (src/fj/projection.fj:217) and frustum_sector (:325) are defined but never invoked, and the required `coarse_bounds` table label is never emitted anywhere in src/ (any invocation would fail assembly, confirming nothing ships through this path); reference_model.py never imports coarse_cull despite the docstring's \"shared (R6 SSOT) by ... the host reference\" claim; wall_renderer.py:519 confirms the feature was priced at +24.7M and rejected. The cited host/fj degenerate divergence is also real: octant_wedge_idx returns None for (0,0) while the fj macro (no guard; smaller=larger=0 makes every s8>=larger*k compare succeed, projection.fj:259-291) computes octant 0/wedge 7 and reads a bogus ~ANG45 interval. However, this is correctly a severity=design finding (R2 untested logic file + overclaiming R6 docstring): the dead code cannot affect any buildable configuration, so no shipped or latent defect exists \u2014 hence needs_code_change=false per the contract (design/style notes get false)."
 },
 {
  "file": "scripts/count_until_full.py",
  "line": 261,
  "rule": "none",
  "severity": "design",
  "claim": "The until-full latch is evaluated only at subsector granularity (top of the subsector loop), so every seg of the subsector during which the last column fills \u2014 plus that whole subsector \u2014 is counted as pre-full work, slightly overstating segs_visited_until_full and understating the post_full percentages that size pG1's walk-abort win; the docstring also claims _seg_sector is called, but sds/secs are loaded and never used.",
  "scenario": "At a viewpoint where the screen fills on the first seg of a 20-seg subsector, all 20 segs (including 19 post-full ones) are tallied into segs_visited_until_full, so post_full_seg_pct reads lower than the true abortable share \u2014 a conservative bias in the sizing numbers, not a renderer defect (the marking semantics themselves are right: drawn covers the half-open [x1,x2) exactly as wall_x_range documents at reference_model.py:480).",
  "needs_code_change": false,
  "unit": "scripts",
  "verdict": "confirmed",
  "v_needs_change": false,
  "v_reason": "Verified in scripts/count_until_full.py (current tree, 101 lines \u2014 the claim's line 261 anchor is wrong; the finding lives at lines 43-62): the all_drawn() latch is checked only at the top of the subsector loop (line 43) and never inside the seg loop, so every seg of the subsector during which the screen fills \u2014 including segs processed after the fill \u2014 is tallied into segs_visited_until_full/segs_passing_xrange_until_full (lines 52-53, 58-59), understating post_full_seg_pct (line 74). This is a conservative bias in a scratchpad measurement script (output to scratchpad/bakeoff/until_full_counts.json), not a renderer defect. The docstring mismatch is also real: line 2 claims _seg_sector is called, but it never is, and sds/secs (lines 24-25) are loaded and unused. The marking semantics are correct as the claim states \u2014 wall_x_range documents half-open [x1,x2) coverage (reference_model.py:478-484), matching the script's range(x1,x2) loop. Design/precision note only; no code change warranted."
 },
 {
  "file": "scripts/walk_e1m1.py",
  "line": 280,
  "rule": "none",
  "severity": "style",
  "claim": "The exception-handler comment says 'step back and carry on' but the code performs no step-back: px/py/ang keep the out-of-map values after a failed render.",
  "scenario": "Walk through a wall so render() throws: the handler only prints 'render failed at (px,py)'; the player position stays at the invalid spot, the window keeps showing the last good frame while the caption/position silently diverge, and every further forward step compounds from the bad coordinates until the user manually reverses -- the comment promises a rollback that was never implemented, misleading the next maintainer.",
  "needs_code_change": false,
  "unit": "scripts",
  "verdict": "confirmed",
  "v_needs_change": false,
  "v_reason": "Verified at scripts/walk_e1m1.py:276-281: px/py/ang are mutated before render() and the except handler only prints -- no rollback exists, so after a failed render the position stays out-of-map, flip()/set_caption are skipped (window and caption show the last good state), and further forward steps compound until the user manually reverses. The comment 'step back and carry on' at best means the user should step back, but reads as a promised rollback that was never implemented. However this is a host-side interactive debug walker outside all certification gates, the behavior is a deliberate best-effort design, and the finding is comment-clarity/style -- not a defect requiring a fix now."
 },
 {
  "file": "scripts/count_until_full.py",
  "line": 24,
  "rule": "none",
  "severity": "style",
  "claim": "Dead code and a lying docstring: locals `sds` (line 24) and `secs` (line 25) are never used, and the module docstring (line 2) claims the script calls the oracle's `_seg_sector`, which appears nowhere in the file; the one-sided-only until-full model (line 49 `if ld.back != -1: continue`) also predates the shipped walk's marking-seg claims and bbox cull, so its post-full percentages describe the M13p0-era walk, not the current one.",
  "scenario": "A maintainer sizing a new walk-abort change trusts the docstring's 'no duplicated math, no drift' claim and this script's post_full_seg_pct numbers: the script counts only one-sided segs toward column claims while the shipped renderer also claims via two-sided marking segs, so the reported post-full share overstates the win; the unused sidedef/sector loads and the phantom _seg_sector reference send the reader hunting for logic that does not exist.",
  "needs_code_change": false,
  "unit": "scripts",
  "verdict": "confirmed",
  "v_needs_change": false,
  "v_reason": "Verified in scripts/count_until_full.py: `sds` (line 24) and `secs` (line 25) are assigned and never used; the docstring (lines 1-2) names `_seg_sector`, which is never called in the file (it exists only in reference_model.py); and line 49's `if ld.back != -1: continue` restricts column claims to one-sided segs, matching the self-declared M13p0-era walk while the current shipped walk (M13-2S+) also claims via two-sided marking segs \u2014 so the script's post_full percentages do not describe the current walk. Minor caveat: the scenario's bias direction (\"overstates the win\") is arguable \u2014 marking-seg claims fill the screen earlier, so the script more likely understates the post-full share \u2014 but the staleness/misleading-doc finding holds regardless. This is a host-side measurement harness outside the certification gates; a style/doc note on a historical script, not a defect requiring a fix now."
 },
 {
  "file": "docs/handoff-m13-2s-fast.md",
  "line": 257,
  "rule": "none",
  "severity": "design",
  "claim": "The \u00a76 ladder still schedules rung 3c-1 as the '2b' pass-2 clip ('start pass 2 at the prescan's x, then the dmax end clip', abort criterion 'take it anyway (it is 3 lines)'), and line 259 gates rung 3c-3 'byte-exact vs 3c-1' \u2014 directly contradicting the same document's \u00a72b (lines 112\u2013127), which records that 2b was MEASURED AND DROPPED, worth \u22640.08M, that the 'three lines' prescan-x half came back NOT byte-exact (scale accumulates on every column), and which says verbatim 'Recorded so nobody rebuilds it.'",
  "scenario": "A future session executes the \u00a76 ladder top-down (the doc's stated purpose \u2014 'each rung measurable and abortable'), reaches the un-struck 3c-1 row, follows its 'take it anyway, it is 3 lines' instruction, and rebuilds the exact change \u00a72b already proved non-byte-exact, burning a ~10-minute build + debug cycle and then targeting 3c-3's stale 'byte-exact vs 3c-1' gate against a baseline that never existed.",
  "needs_code_change": true,
  "unit": "docs",
  "verdict": "confirmed",
  "v_needs_change": false,
  "v_reason": "All cited text verified in the current tree of docs/handoff-m13-2s-fast.md: line 112 strikes \u00a72b as \"MEASURED AND DROPPED\" (prefix-skip half built and NOT byte-exact, \"Recorded so nobody rebuilds it\" at line 127), yet the \u00a76 ladder row 3c-1 at line 257 still schedules 2b un-struck with \"take it anyway (it is 3 lines)\", and line 259 gates 3c-3 \"byte-exact vs 3c-1\". Commit 71d3536 added the DROPPED section and fixed the \u00a77 estimates but never updated the ladder \u2014 a real internal contradiction. However it is doc-only (severity design, rule none): the ladder row's \"2b\" reference leads straight to the DROPPED banner, the \"3 lines\" claim lives in the explicitly-superseded 2b-OLD subsection, and M13 is closed per the project memory so the top-down-execution scenario is unlikely. Worth a one-line strikethrough on next doc touch, not a fix-now code change."
 },
 {
  "file": "docs/handoff-m13-2s-fast.md",
  "line": 242,
  "rule": "R6",
  "severity": "design",
  "claim": "STEP_SEG_BUDGET is stated three inconsistent ways inside one handoff \u2014 line 159 ('capped at STEP_SEG_BUDGET = 24'), line 230 ('only from the nearest 24 marking segs'), line 242 reserve lever 2 ('STEP_SEG_BUDGET 24 \u2192 12: ~\u22120.4M') \u2014 while line 178's own correction and the \u00a73 projection (line 204, '+ 2c step faces \u2026 budget 12') already assume 12, making lever 2 a double-count of an already-spent saving; and the code SSOT (src/doomfj/reference_model.py:69) has since moved to 8, so every value in the doc is stale.",
  "scenario": "A session hunting ops under the owner's ceiling reads the \u00a75 'reserve levers, in the order to spend them' list, takes lever 2's ~\u22120.4M as available headroom, and plans/promises a budget that is impossible: the 24\u219212 saving is already baked into the \u00a73 \u224821.3M/\u224827.1M totals, and the shipped tree runs budget 8 \u2014 so the 'reserve' delivers zero (or the reader re-raises 8\u219212 believing it restores a documented baseline, changing the shipped picture).",
  "needs_code_change": true,
  "unit": "docs",
  "verdict": "confirmed",
  "v_needs_change": false,
  "v_reason": "confirmed"
 },
 {
  "file": "docs/handoff-session-visuals.md",
  "line": 280,
  "rule": "R6",
  "severity": "design",
  "claim": "Section 5a ('What is left, in the order I would do it') states 'THING_BUDGET is 16' and its item 3 (line ~296) reasons about the walk ending 'sooner at 16 than at 24', contradicting the same file's \u00a75 marked 'read this first' (lines 163\u2013172: 'no count limit: both THING_BUDGET and MONSTER_BUDGET sit at 255 \u2026 cannot bind') and the code SSOT (src/doomfj/reference_model.py:156, THING_BUDGET = 255; :144, MONSTER_BUDGET = 255, owner decision 2026-08-01).",
  "scenario": "A future session picks up \u00a75a's ranked to-do list (its stated function), re-prices the project_thing lever using item 3's premise that a budget of 16 already truncates the walk, and either skips the lever as pre-paid or measures against a nonexistent configuration \u2014 the shipped renderer projects every thing that passes the min-size reject, so the lever's real value is materially different from what \u00a75a implies.",
  "needs_code_change": true,
  "unit": "docs",
  "verdict": "confirmed",
  "v_needs_change": false,
  "v_reason": "docs/handoff-session-visuals.md:280 states \"THING_BUDGET is 16\" and item 3 (line 295) reasons \"the budget still ends the walk sooner at 16 than at 24\", while the same file's authoritative \u00a75 (lines 163-171, \"read this first\", dated 2026-08-01) says both budgets sit at 255 and cannot bind, matching the code SSOT (src/doomfj/reference_model.py:156 THING_BUDGET=255, :144 MONSTER_BUDGET=255, owner decision 2026-08-01). \u00a75a is an un-revised pre-decision remnant (EXP-8's 16, see the line-17 table) yet presents itself as the live ranked to-do list, so a future session re-pricing the project_thing reject lever would use a false premise \u2014 nothing truncates the thing walk in the shipped config, so the lever's value differs materially from what \u00a75a implies. Doc-internal contradiction against the SSOT (R6 in spirit), severity design; a doc correction, not a code change."
 },
 {
  "file": "docs/handoff-session-visuals.md",
  "line": 280,
  "rule": "R6",
  "severity": "design",
  "claim": "Section 5a states \"`THING_BUDGET` is **16**\" as a completed item, contradicting both the same file's authoritative end-of-session section (line 164: both budgets \"sit at 255 ... and cannot bind\", owner decision 2026-08-01) and the code (src/doomfj/reference_model.py:156 sets THING_BUDGET = 255).",
  "scenario": "A future session resuming from this handoff reads section 5a, believes the shipped sprite selection is count-limited at 16, and either re-derives frame-cost budgets or 'restores' the 16 cap \u2014 silently reverting the owner's distance-only selection policy that guarantees monsters are never dropped by arrival order.",
  "needs_code_change": false,
  "unit": "docs",
  "verdict": "confirmed",
  "v_needs_change": false,
  "v_reason": "Verified: docs/handoff-session-visuals.md:280 states THING_BUDGET is 16 as a completed item, directly contradicting the same file's authoritative section (lines 163-165: both budgets sit at 255 and cannot bind, owner decision 2026-08-01) and the code (src/doomfj/reference_model.py:156: THING_BUDGET = 255, comment 'Was 16 (EXP-8); RETIRED'). The contradiction extends beyond line 280: section 5a item 3 (lines 294-296) bases planning advice on the 16 cap ('the budget still ends the walk sooner at 16 than at 24'), so a future session executing that to-do list would mis-price the project_thing lever or restore the retired count cap, reverting the owner's distance-only sprite policy. This is a real R6 doc-consistency defect, but it is documentation-only \u2014 shipped code behaves as certified \u2014 so it is severity=design with no code change needed (fix is editing section 5a)."
 },
 {
  "file": "docs/handoff-session-visuals.md",
  "line": 148,
  "rule": "R6",
  "severity": "style",
  "claim": "The starred \"THE PLAYABLE DEFAULT IS NOW E2M8, NOT E1M1\" claim (walker defaults to assets/freedoom1.wad E2M8) no longer matches the tree: scripts/walk_e1m1.py:57-58 defaults to tests/fixtures/e1m1_lite.wad / E1M1, and its --wall-mode default is W1R, not the WPX tier this doc describes.",
  "scenario": "Someone following this \"read this first\" section to reproduce the documented 24.5M / 5.6 fps arena numbers runs `python scripts/walk_e1m1.py` bare, actually gets the E1M1 W1R build, and misattributes the ~2x cost difference to a regression rather than to the changed defaults.",
  "needs_code_change": false,
  "unit": "docs",
  "verdict": "confirmed",
  "v_needs_change": false,
  "v_reason": "Verified: docs/handoff-session-visuals.md:148-153 (starred \"read this first\" state section) says the playable default is assets/freedoom1.wad E2M8 (24.5M / 5.6 fps, WPX tier per line 65), but scripts/walk_e1m1.py:57-58 defaults to tests/fixtures/e1m1_lite.wad / E1M1 and line 65 defaults --wall-mode to W1R (WPX is opt-in, +2-6M/frame). The doc was superseded by later owner decisions recorded only in the script's comments (2026-08-01/02), so someone reproducing the documented numbers from a bare run gets a different build and could misread the delta as a regression. It is a stale-doc/style finding, not a code or certification defect \u2014 no runtime behavior is wrong \u2014 so no code change is required now (a doc correction would suffice)."
 },
 {
  "file": "docs/handoff-m13-2s-fast.md",
  "line": 178,
  "rule": "R6",
  "severity": "style",
  "claim": "The emphatic \"\u26a0 `STEP_SEG_BUDGET` is 12, not 24\" (and \"STEP_SEG_BUDGET = 12\" at docs/handoff-visual-features.md:106 and :281) is wrong against the shipped code: src/doomfj/reference_model.py:69 sets STEP_SEG_BUDGET = 8 (passed into the fj emit at wall_renderer.py:1302), retuned to 8 in the later DEG-knob campaign (docs/opt-experiments.md:490).",
  "scenario": "These are the designated resume-from docs for rung-3c/V3 work; a session re-pricing the worst viewpoint uses the documented budget of 12 x 93k per face-carrying seg (the doc calls it \"the dominant term\") and over-budgets step-face cost by 50%, or \"restores\" 12 believing 8 is a drift bug rather than an owner-tuned knob.",
  "needs_code_change": false,
  "unit": "docs",
  "verdict": "confirmed",
  "v_needs_change": false,
  "v_reason": "Verified against the working tree: docs/handoff-m13-2s-fast.md:178 (\"\u26a0 STEP_SEG_BUDGET is 12, not 24\"), plus :242, and docs/handoff-visual-features.md:106, :281 (and :319) all state 12, while src/doomfj/reference_model.py:69 sets STEP_SEG_BUDGET = 8, imported at wall_renderer.py:35 and baked into the fj emit at wall_renderer.py:1302 \u2014 so the shipped/certified renderer uses 8 and the resume-from docs are stale. git log -L on that line shows it was created as 12 (700de4a, the same era the handoff docs were written) and changed to 8 in the DEG-knob ship commit 8f30eb6 (\"20M RECOVERED\"), matching the memory record (\"STEP 8, sliver 3, PNEAR 64\"). One nuance in the reviewer's evidence: the cited opt-experiments.md:490 row (PNEAR 64 + STEP 8) is explicitly marked \"NOT SHIPPED\" in that doc \u2014 that table records an earlier standalone experiment; the retune to 8 actually shipped later as part of the 8f30eb6 package \u2014 but this weakens only the citation, not the finding: the doc/code mismatch is real and independently verified. The failure scenario is plausible (a resume session over-prices step-face cost by 50% at 12x93k, or \"restores\" 12 as presumed drift). It is a documentation-drift/R6 style finding with zero effect on the certified binary, so no code change is required; the fix is a doc correction (update the three doc sites to 8, ideally noting it is an owner-tuned DEG knob)."
 },
 {
  "file": "tests/fj/stream_screen.py",
  "line": 272,
  "rule": "none",
  "severity": "latent",
  "claim": "The 0x0B collines DITTO handler copies column x-1 with no x>0 guard, so a ditto record for column 0 silently reads Python negative indices (y*width-1, i.e. the last pixel of the PREVIOUS row, and index -1 = the framebuffer's last pixel for y=0) instead of raising.",
  "scenario": "The certified fj emitter never emits ditto at x=0, so shipped gates are unaffected; but if a future emitter regression (or the planned flipjump upstreaming of this device) ever produces [0x00][0xFE], the device renders plausible-looking wraparound garbage rather than failing at the decode site \u2014 the byte-exact compare then fails one frame later with a mystifying whole-column pixel diff instead of pointing at the malformed record. A one-line `assert self._cl_x > 0` would localize it.",
  "needs_code_change": false,
  "unit": "tests-0",
  "verdict": "confirmed",
  "v_needs_change": false,
  "v_reason": "The claim is factually accurate: tests/fj/stream_screen.py:269-274 handles the 0x0B DITTO tag (0xFE) with no x>0 guard, and pixel_indices is a plain Python list, so a ditto at column 0 silently reads negative/wrapped indices (y*width-1; index -1 for y=0 = the framebuffer's last pixel) instead of raising. The premise that no certified path emits it is also verified in the fj source: the shipped lines leaf (src/fj/frame_render.fj) gates ditto on dvalid, which is zeroed at every seg entry (line 2525) and on gaps (line 2538) and set only after an emitted column (line 2760), so column 0 can never ditto; the rung-3b flush_frame (src/fj/stream_render.fj:639) has an explicit `hex.if0 2, fx, colfull` column-0 guard. However, the only consequence is degraded diagnostics on protocol-violating input from a hypothetical future emitter bug \u2014 the byte-exact compare still fails (one frame later, with a mystifying column diff), so no wrong result is ever silently accepted. That makes this a robustness/diagnosability note on test tooling (rule: none; the suggested one-line assert is reasonable hardening), not a real or latent defect worth a fix-now flag under the review contract."
 },
 {
  "file": "tests/fj/stream_screen.py",
  "line": 49,
  "rule": "R6",
  "severity": "design",
  "claim": "Lines 49-53 re-hardcode LIGHTZSHIFT=20, MAXLIGHTZ=128, LIGHTLEVELS=16, LIGHTSEGSHIFT=4, COLORMAP_LIGHTS=32 as local literals instead of importing them from doomfj.tables/reference_model \u2014 the very SSOT rule the same file's header invokes to justify importing slopediv_recip_table from doomfj.tables (and which test_plane_bands_fj.py follows by importing COLORMAP_LIGHTS from reference_model).",
  "scenario": "A future light-resolution change (e.g. MAXLIGHTZ or LIGHTZSHIFT retuned in src/doomfj/tables.py) updates the oracle and the fj LUT generators together but leaves this device decoding zlight with stale bucket geometry; every raster/proj/stream gate then fails with opaque per-row colour diffs (or, worse, a paired copy-edit keeps them 'matching' while both drift from the fj tables), instead of the constant change propagating automatically.",
  "needs_code_change": false,
  "unit": "tests-0",
  "verdict": "confirmed",
  "v_needs_change": false,
  "v_reason": "tests/fj/stream_screen.py:49-53 does re-hardcode LIGHTZSHIFT/MAXLIGHTZ/LIGHTLEVELS/LIGHTSEGSHIFT/COLORMAP_LIGHTS as load-bearing literals (used at lines 165-166, 381-382, 426 for DMA sizing, zlight indexing, and the bucket shift) while src/doomfj/tables.py:15-18 and reference_model.py:225 define the identical constants, the file already imports from doomfj.tables (line 29) with a header (lines 24-28) explicitly invoking SSOT to justify that import, and test_plane_bands_fj.py:14 shows the import precedent \u2014 so the R6 claim is accurate. However, a tables.py retune would produce LOUD byte-exact gate failures (wrong DMA length/stride/shift on the device side vs regenerated fj LUTs), not silent divergence \u2014 the fj tables flow from tables.py so lockstep drift is implausible \u2014 and four other test files hardcode COLORMAP_LIGHTS=32 the same way. This is a maintainability/R6-hygiene design finding (the reviewer's own stated severity), not a real or latent defect in current behavior, so needs_code_change=false per the contract's design-note rule."
 },
 {
  "file": "tests/fj/test_plane_bands_fj.py",
  "line": 124,
  "rule": "R6",
  "severity": "design",
  "claim": "The build_bands gate hardcodes the screen geometry and light-bucket math \u2014 `cfg_centery = 50` and `100 - cfg_centery` (line 131) instead of Config().CENTERY / Config().VIEW_H, and `lvl = min(15, light >> 4)` (line 64) instead of min(LIGHTLEVELS-1, light >> LIGHTSEGSHIFT) \u2014 duplicating constants the same file already pulls from Config for its LUT generators (lines 141-143).",
  "scenario": "If VIEW_H (or LIGHTSEGSHIFT) ever changes, the fj kernel and its LUTs are driven at the new geometry while the test windows stay (y0=0,count=50)/(y0=50,count=50) and the host `_expected_bands` mirror uses the same stale numbers \u2014 both sides agree, the test PASSES, but the gate silently stops exercising the real ceiling/floor windows (e.g. it never reaches rows 50..H-1), exactly the silent-coverage-loss failure mode R6 exists to prevent.",
  "needs_code_change": false,
  "unit": "tests-0",
  "verdict": "confirmed",
  "v_needs_change": false,
  "v_reason": "tests/fj/test_plane_bands_fj.py hardcodes cfg_centery=50 (line 124), the 100-row screen height (line 131, plus the ph-zero test at line 142), and lvl=min(15, light>>4) (line 64), duplicating Config().CENTERY/VIEW_H and tables.py's LIGHTLEVELS/LIGHTSEGSHIFT \u2014 while the same file already pulls Config().VIEW_W/VIEW_H for its LUT generators (lines 116-117; the claim's \"141-143\" cite was off but immaterial). The scenario holds: y0/count are plain kernel inputs, so a VIEW_H or LIGHTSEGSHIFT change regenerates the LUTs at new geometry while the test windows and host mirror keep the stale numbers, both sides agree, and the gate silently stops covering the real ceiling/floor windows (R6 silent-coverage-loss). Design/test-hygiene finding \u2014 values are correct at the certified 160x100 config, so no defect today; per contract, design notes get needs_code_change=false."
 },
 {
  "file": "tests/fj/test_floor_planes_fj.py",
  "line": 185,
  "rule": "R6",
  "severity": "style",
  "claim": "The capstone docstring still says the frame must match 'the published M13b E1M1 spawn golden db5d3da8', but the constant it actually asserts (E1M1_TEX_GOLDEN, line 41) was re-blessed to b7a28e38... for perf #9+#11 \u2014 the comment lies about which golden the test enforces.",
  "scenario": "During the next re-bless investigation someone greps for db5d3da8, finds this docstring, and concludes the textured golden moved twice (or 'restores' db5d3da8 believing it the published value), wasting a diagnosis cycle on a hash that stopped being the gate's truth at the perf #9+#11 re-bless.",
  "needs_code_change": false,
  "unit": "tests-0",
  "verdict": "confirmed",
  "v_needs_change": false,
  "v_reason": "Verified in the working tree: tests/fj/test_floor_planes_fj.py:185 docstring says the frame must match \"the published M13b E1M1 spawn golden db5d3da8\", but the test's actual gate (line 235) asserts E1M1_TEX_GOLDEN, defined at line 41 as b7a28e38... with a \"perf #9+#11 [re-bless]\" comment; grep confirms db5d3da8 appears nowhere else in the repo's Python (host goldens at tests/host/test_floor_planes.py:124 and tests/host/test_wall_frame.py:114 both assert b7a28e38...). The docstring is a stale, dangling hash reference that would mislead a future re-bless investigation exactly as the scenario describes. Behavior and certification are unaffected \u2014 the assertion enforces the correct re-blessed golden \u2014 so this is a documentation/style finding (R6 doc-as-second-source-of-truth divergence), not a defect: needs_code_change=false per the contract."
 },
 {
  "file": "tests/fj/stream_screen.py",
  "line": 272,
  "rule": "none",
  "severity": "latent",
  "claim": "The 0x0B DITTO handler copies pixel_indices[y*width + x - 1] with no guard for x == 0: Python's negative indexing makes row 0 silently copy the framebuffer's LAST pixel (index -1) and every other row copy the previous row's last column, instead of rejecting the malformed record.",
  "scenario": "If the fj emitter's ditto-key machinery (actively retuned \u2014 the 'widened ditto key' of rung 3a/3b) ever regresses and emits a ditto record for column 0, the device decodes a plausible-looking frame with wrap-around garbage instead of failing loudly at the protocol violation, so the gate reports an inscrutable scattered pixel diff rather than 'ditto at column 0'. A one-line assert self._cl_x > 0 turns a debugging session into an instant diagnosis; no current emitter path triggers it, which is why the certified sweeps never see it.",
  "needs_code_change": false,
  "unit": "tests-0",
  "verdict": "confirmed",
  "v_needs_change": false,
  "v_reason": "The code claim is accurate: tests/fj/stream_screen.py:272 (the 0xFE DITTO branch \u2014 the claim's \"0x0B\" label is a typo, line and substance match) copies pixel_indices[y*width + x - 1] with no x>0 guard, and the tag path at line 261 accepts x=0, so a malformed [0x00][0xFE] record would silently wrap (row 0 reads index -1, other rows read the prior row's last column) instead of failing loudly. However, the fj emitter explicitly guards column 0 (src/fj/stream_render.fj:639, \"hex.if0 2, fx, colfull // column 0 has no left neighbour\"), so no current build can produce the record \u2014 consistent with the byte-exact certification. The device is documented as decision-free by design (stream_screen.py:252) and every other record handler in the harness equally trusts the emitter with no bounds checks; the proposed assert is a diagnosability hardening of a test harness against a hypothetical future emitter regression, i.e. a robustness/design note, not a latent defect requiring a fix now."
 },
 {
  "file": "tests/fj/stream_screen.py",
  "line": 544,
  "rule": "none",
  "severity": "style",
  "claim": "x1c, x2c = max(0, x1), min(self.width, x2) in _proj_process_seg is dead code \u2014 the loop below iterates range(x1, x2) with an inline 0 <= x < self.width bounds check and never reads x1c/x2c.",
  "scenario": "A reader assumes the clamped bounds are what the loop uses and 'simplifies' to range(x1c, x2c), which changes the scale accumulator's starting column (the adjacent comment explains DOOM accumulates rw_scale from the unclamped x1) and breaks the byte-exact contract for off-screen-left segs; deleting the two dead names removes the trap.",
  "needs_code_change": false,
  "unit": "tests-0",
  "verdict": "confirmed",
  "v_needs_change": false,
  "v_reason": "tests/fj/stream_screen.py:544 assigns x1c/x2c which appear nowhere else in the file (grep: single occurrence); the loop at line 547 iterates range(x1, x2) with an inline 0 <= x < self.width check and accumulates scale from the unclamped x1 per the adjacent comment, so the clamped names are dead and do constitute a simplification trap \u2014 but current behavior is correct, so it is a style note only."
 },
 {
  "file": "tests/host/test_mapbake.py",
  "line": 243,
  "rule": "R3",
  "severity": "design",
  "claim": "The M12qq self-zero involution test is vacuous for the NEW M13-possignmag sign_dx/sign_dy registers (and for cpx/cdx_mag): every _deep_map node has non-negative dx/dy (dx=1,1,0; dy=0,0,1 at test_mapbake.py:159-161), so the sign flags' SET values are all 0 and their post-walk xor is 0 whether or not the SET/CLEAR involution runs at all \u2014 and the docstring even miscites 'sign_dy = 0 ^ 0 ^ 0' as its example of a NONZERO residue (the actual catcher is cdy_mag = 0^0^1).",
  "scenario": "A mapcompiler regression that emits the sign_dx/sign_dy hex.xor_by lines in the SET block but drops them from the CLEAR block (mapcompiler.py emits the xor lines at two sites, ~369-372 and ~463-466) leaves stale sign flags mid-walk on any map with a negative partition delta, flipping point_on_side decisions for later nodes \u2014 yet this dedicated regression test passes, because on this fixture the sign accumulation is 0 either way; only the coarser E1M1 order test could catch it, defeating the test's stated purpose of 'directly' catching a broken involution. Fix: give one _deep_map node a negative dx or dy (odd count) and correct the docstring's example.",
  "needs_code_change": false,
  "unit": "tests-1",
  "verdict": "confirmed",
  "v_needs_change": false,
  "v_reason": "Confirmed: on the _deep_map fixture (test_mapbake.py:159-161, dx=1,1,0 / dy=0,0,1, all non-negative) the sign_dx/sign_dy SET values are all 0 (mapcompiler.py:465-466), so their post-walk xor is 0 with or without the involution \u2014 cpx (0^0^0) and cdx_mag (1^1^0) are equally vacuous; only cpy and cdy_mag carry a nonzero residue. The docstring at test_mapbake.py:242-243 does miscite sign_dy = 0^0^0 as its example of a nonzero residue (the real catcher is cdy_mag = 0^0^1). One correction to the reviewer's mechanism: the two emission sites (~367-372 and ~458-466) are the pruned-node vs normal-node variants of the SAME xb{i} block, and SET/CLEAR are two fcalls to that one block (lines 446-448), so 'sign lines in SET but dropped from CLEAR' cannot arise from those sites \u2014 but the equivalent regression (sign xor lines dropped from the xb block entirely, or a refactor splitting sign handling like the inline_side path) leaves stale/never-set sign flags and still passes every fixture-level test including this one, defeating its stated 'directly catches a broken involution' purpose. Severity design (R3 test-coverage + docstring): no shipped behavior is wrong and E1M1 certification exercises real negative deltas, so needs_code_change=false; the cheap fix (one negative-delta node + docstring correction) is still worth taking when the file is next touched."
 },
 {
  "file": "tests/host/test_plane_bands.py",
  "line": 7,
  "rule": "none",
  "severity": "style",
  "claim": "The module docstring is stale and contradicts this same branch: it says _zidx_band_walk is 'Not yet consumed by _render_planes_flat (still exact, byte-identical to the shipped flat goldens)' and points at _render_planes_flat's docstring 'for why the wiring waits' \u2014 but reference_model.py:2242-2258 (M13pS2-crush2b) now wires the band walk INTO _render_planes_flat, and tests/host/test_floor_planes.py:141 re-blesses the flat-colored E1M1 golden for exactly that (79/16,000 pixels shifted).",
  "scenario": "A maintainer investigating a flat-tier golden mismatch reads this docstring, concludes the flat path is still per-pixel exact, and misdiagnoses a legitimate <=1-row F4 band-edge shift as a real regression (or vice versa re-blesses a real bug believing approximation is expected) \u2014 the two files in the same branch assert opposite exactness contracts for the shipped flat goldens.",
  "needs_code_change": false,
  "unit": "tests-1",
  "verdict": "confirmed",
  "v_needs_change": false,
  "v_reason": "Verified in the working tree: tests/host/test_plane_bands.py:7-9 claims _zidx_band_walk is \"Not yet consumed by _render_planes_flat (still exact, byte-identical to the shipped flat goldens)\", but src/doomfj/reference_model.py wires it in on this same branch \u2014 _render_planes_flat (lines 2270/2276) calls _flat_row_colours, which calls self._zidx_band_walk (line 2218), and its own docstring (2242-2258, M13pS2/crush2b) documents the F4 <=1-row approximation as applied; tests/host/test_floor_planes.py:141 re-blesses the E1M1 flat golden for exactly that shift. Branch history confirms the mechanism: the docstring was written at 739f8a8 (pS2a, pre-wiring) and never updated when crush2b landed. The contradiction is real and the misdiagnosis scenario plausible, but it is a stale docstring only \u2014 behavior, goldens, and the certified config all agree \u2014 so it is a style/docs fix, not a code defect."
 },
 {
  "file": "tests/host/test_plane_bands.py",
  "line": 21,
  "rule": "R6",
  "severity": "style",
  "claim": "_exact_zidx hardcodes 127 and 20 in 'min(127, dist >> 20)' instead of importing MAXLIGHTZ/LIGHTZSHIFT from doomfj.tables (the SSOT reference_model.py itself imports at line 38 and uses at line 1339 as 'min(MAXLIGHTZ - 1, ... >> LIGHTZSHIFT)'), so the test's definition of 'the always-exact formula' is a duplicated constant pair.",
  "scenario": "If MAXLIGHTZ or LIGHTZSHIFT is ever retuned in tables.py (e.g. a coarser zlight bucket count for a cheaper zlight table), _zidx_band_walk follows the SSOT but this comparator silently keeps encoding 128-buckets/>>20 \u2014 the F4 bound test then fails against a formula that is no longer the exact per-pixel one, sending the debugger after a phantom band-walk bug instead of a one-line constant import.",
  "needs_code_change": false,
  "unit": "tests-1",
  "verdict": "confirmed",
  "v_needs_change": false,
  "v_reason": "Verified: tests/host/test_plane_bands.py:21 hardcodes min(127, dist >> 20), duplicating tables.py's MAXLIGHTZ=128/LIGHTZSHIFT=20, which reference_model.py imports (line 38) and uses in the exact formula the test claims to mirror (line 1339: min(MAXLIGHTZ - 1, ... >> LIGHTZSHIFT)). _zidx_band_walk uses the imported constants, so a retune in tables.py would silently desynchronize this comparator and make the F4 bound test fail against a stale formula \u2014 a genuine R6 duplicated-constant issue. However, it is a host-test hygiene/style note: current values match, no shipped or latent runtime defect exists, so needs_code_change=false per the design/style rule."
 },
 {
  "file": "tests/host/test_plane_bands.py",
  "line": 7,
  "rule": "none",
  "severity": "design",
  "claim": "The module docstring states _zidx_band_walk is 'Not yet consumed by _render_planes_flat (still exact, byte-identical to the shipped flat goldens)', but on this branch src/doomfj/reference_model.py:2242 wires the band walk into _render_planes_flat, and this same diff re-blesses the E1M1 flat golden for that wiring (tests/host/test_floor_planes.py:141, 'M13pS2 F4 [re-bless]: SHARED full-range band-walk zidx').",
  "scenario": "A developer investigating a flat-tier pixel diff reads this docstring, concludes the <=1-row band-walk drift cannot be the cause because 'the oracle output is still exact', and wastes the debugging session elsewhere -- the exact comments-that-lie maintainability trap; the stale two sentences should be deleted or rewritten to say the wiring landed with the F4 re-bless.",
  "needs_code_change": false,
  "unit": "tests-1",
  "verdict": "confirmed",
  "v_needs_change": false,
  "v_reason": "The module docstring at tests/host/test_plane_bands.py:7-9 (\"Not yet consumed by _render_planes_flat ... still exact, byte-identical to the shipped flat goldens\") is stale on this branch: _flat_row_colours calls _zidx_band_walk at src/doomfj/reference_model.py:2218 and _render_planes_flat consumes it for every ceiling/floor column (lines 2270/2276), _render_planes_flat's own docstring (lines 2242-2249) explicitly documents the F4 wiring with a measured 79/16,000-pixel drift, and the same diff re-blesses the E1M1 flat golden at tests/host/test_floor_planes.py:141 with the \"M13pS2 F4 [re-bless]: SHARED full-range band-walk zidx\" comment. The docstring was true at pS2a when the file was created but the later F4 rung on the same branch wired the walk in without updating it. Purely a documentation contradiction (behavior is as certified), so per the contract this is a design/maintainability note \u2014 needs_code_change=false \u2014 but the two stale sentences should be rewritten to say the wiring landed with the F4 re-bless."
 },
 {
  "file": "scratchpad/make_arena.py",
  "line": 53,
  "rule": "R6",
  "severity": "design",
  "claim": "args.roof is parsed but never used: the SECTORS lump hardcodes CEIL3_5 as the ceiling flat, so the F_SKY1 default and the 'open-air keeps V2 sky on the shipped path' help text lie about what the tool builds.",
  "scenario": "After the syntax error is fixed, a user runs the tool expecting an open-air F_SKY1 arena (or passes --roof explicitly); the generated wad silently gets a solid CEIL3_5 ceiling, so the V2 sky path the docstring promises to exercise is never on screen.",
  "needs_code_change": true,
  "unit": "scratchpad-0",
  "verdict": "confirmed",
  "v_needs_change": false,
  "v_reason": "Verified in scratchpad/make_arena.py: 'roof' appears only at line 31 (the argparse declaration) \u2014 args.roof is never read \u2014 and SECTORS (lines 52-53) hardcodes b\"CEIL3_5\" as the ceiling flat, so the F_SKY1 default and the 'open-air / V2 sky on the shipped path' help text describe behavior the tool lacks. The premise syntax error is also real (ast.parse: unterminated string literal at line 32, the multi-line help string), so the file cannot run today and was never touched by the byte-exact certification gates. The claim holds exactly, but this is a scratchpad host tool at the reviewer's own severity=design, currently non-executable, so it is a design/doc-truth note rather than a defect requiring a fix now."
 },
 {
  "file": "scratchpad/band_compare.py",
  "line": 16,
  "rule": "R6",
  "severity": "style",
  "claim": "W, H = 160, 100 hardcodes the view dimensions instead of taking cfg.VIEW_W/VIEW_H like every other probe in this diff (e.g. explain_1869.py, bucket_probe.py), duplicating the single source of truth.",
  "scenario": "If the resolution config ever changes, band_compare's save() writes a garbled image and the W*H diff percentage is computed over the wrong pixel count, silently misreporting the band-fidelity comparison. Probe-only, no shipped impact.",
  "needs_code_change": false,
  "unit": "scratchpad-0",
  "verdict": "confirmed",
  "v_needs_change": false,
  "v_reason": "Verified: scratchpad/band_compare.py:16 hardcodes W, H = 160, 100 while sibling probes in the same diff (explain_1869.py:21, bucket_probe.py:45,141) use cfg.VIEW_W/VIEW_H, matching R6's \"nothing hardcodes 160/100\" text. The scenario holds: on a resolution change save() reads the frame with a wrong stride and line 57's diff percentage divides by the wrong pixel count. But the file is a consumer-free scratchpad probe (no imports from src/tests/scripts), currently correct at the certified 160x100 config, so this is a style/consistency note \u2014 not a defect requiring a fix now."
 },
 {
  "file": "scratchpad/sector_occl.py",
  "line": 50,
  "rule": "none",
  "severity": "style",
  "claim": "vsector() contains a dead first loop (body is pass, condition references BAMS[best] with best permanently None via an inline ternary guard) that never contributes to the result; the real answer is the diffs.index(min(diffs)) line below.",
  "scenario": "No wrong output today, but a future editor 'fixing' the dead loop into activity (e.g. assigning best) would silently change vertex-sector classification and corrupt the occlusion-cull catch-rate numbers this probe reports.",
  "needs_code_change": false,
  "unit": "scratchpad-1",
  "verdict": "confirmed",
  "v_needs_change": false,
  "v_reason": "Verified at scratchpad/sector_occl.py:44-50: the first loop is genuinely dead \u2014 best is initialized to None and never assigned, the condition parses as `(cmp) if best is not None else True` so the BAMS[best] subexpression is never evaluated (no crash) and the condition is always True with a `pass` body; the function's only return path is diffs.index(min(diffs)) at line 50, and the line-48 comment marks the loop as an abandoned first attempt. The latent-hazard scenario (a future editor activating the loop changes vertex-sector classification feeding the provably_occ cull and the printed catch-rate numbers) holds, though the in-file assert at line 119 would catch a wrong-cull at runtime, softening the 'silent' part for cull correctness (not for the counters). This is a host-side scratchpad probe, not shipped renderer code, so it does not touch the byte-exact certification; style/dead-code note only, no fix-now defect."
 },
 {
  "file": "scratchpad/sprite_audit.py",
  "line": 84,
  "rule": "R7 (comments must not lie)",
  "severity": "design",
  "claim": "The audit's docstring promises a ground-truth vs recorded comparison with SLOTS/MINSIZE/OTHER miss classification, but the code sets recorded identical to gt_visible (tot['recorded'] += accepted) and never updates miss_slots/miss_minsize/miss_other, so it can only ever report the monotone-stop count and would mask slot/minsize sprite losses entirely.",
  "scenario": "A future V4b slot-protocol regression drops visible sprites; the audit is run to verify the owner's 'all sprites always shown' guarantee and prints recorded == gt_visible with zero slot misses, giving a false clean bill because those metrics are structurally incapable of diverging.",
  "needs_code_change": true,
  "unit": "scratchpad-1",
  "verdict": "confirmed",
  "v_needs_change": false,
  "v_reason": "Verified in scratchpad/sprite_audit.py: line 84 sets tot['recorded'] += accepted, the same value as gt_visible (line 83), so the two can never diverge; miss_slots/miss_minsize/miss_other are declared (lines 67-68) but never incremented anywhere; the docstring's promised wall-occlusion ground truth and SLOTS/MINSIZE/OTHER classification are unimplemented, and the report (lines 91-95) prints only accepted-count and th_claim_stopped. The only honest metric is miss_stopped (a real renderer stat), so the audit can detect monotone-stop regressions but is structurally blind to slot/minsize losses \u2014 exactly the reviewer's scenario. R7 violation (docstring lies about capability). It is a scratchpad host diagnostic outside the byte-exact gates, filed as design severity, so needs_code_change=false per the contract; the fix (implement the classification or trim the docstring/dead counters) is worthwhile but not a shipped-code defect."
 },
 {
  "file": "scratchpad/thing_census.py",
  "line": 29,
  "rule": "R6",
  "severity": "design",
  "claim": "MONSTERS/PICKUPS/STARTS are re-hardcoded locally instead of importing MONSTER_TYPES etc. from doomfj.reference_model (which sibling script policy_probe.py correctly imports), so the census's monster/decor categorization can silently diverge from the renderer's real monster policy.",
  "scenario": "MONSTER_TYPES in reference_model gains or loses a type (e.g. a Freedoom-specific id); thing_census still classifies by its stale local set and reports 'budget 16 turns away 0 monsters' while the real DEG monster-always path treats the thing differently, steering a knob decision with wrong data.",
  "needs_code_change": false,
  "unit": "scratchpad-1",
  "verdict": "confirmed",
  "v_needs_change": false,
  "v_reason": "Confirmed: thing_census.py:29 hardcodes MONSTERS locally while reference_model.py:142 defines MONSTER_TYPES (the set the real renderer policy at line 1512 actually uses), and sibling policy_probe.py:18 correctly imports it \u2014 so the census categorization can silently go stale if MONSTER_TYPES changes. But the two sets are currently element-for-element identical, the script is a scratchpad diagnostic whose set only labels print output (no effect on shipped/certified rendering), and PICKUPS/STARTS have no reference_model counterpart to import anyway. Valid R6-spirit design note, not a defect requiring a fix now."
 },
 {
  "file": "scratchpad/sector_occl.py",
  "line": 46,
  "rule": "none",
  "severity": "style",
  "claim": "vsector() contains a dead for-loop whose body is `pass` and whose condition indexes BAMS[best] while best is permanently None (it would raise TypeError if the condition's else-branch didn't short-circuit it to True); the real work is done by the diffs/min lines below, so the loop is confusing dead code with a misleading shape.",
  "scenario": "A reader (or a later edit that gives the loop a real body) trusts the loop as the classifier, e.g. removes the 'simpler:' fallback lines, and vsector then returns None/raises for every vertex, breaking the pre-cull prototype's catch-rate numbers.",
  "needs_code_change": false,
  "unit": "scratchpad-1",
  "verdict": "confirmed",
  "v_needs_change": false,
  "v_reason": "Verified at scratchpad/sector_occl.py:44-50: the for-loop body is `pass`, `best` is never assigned so it stays None, and the condition parses as `(cmp) if best is not None else True` \u2014 the else-branch short-circuits to True on every iteration, which is exactly why BAMS[best] never raises TypeError. The real classifier is the diffs/min fallback on lines 49-50 (correct per the docstring), and the line-48 \"simpler:\" comment shows the loop was abandoned mid-write. All claim details hold. However, this is dead code in a host-side scratchpad measurement prototype whose printed results are correct; nothing shipped or certified touches it, so it is a style/cleanup note, not a defect requiring a change now."
 },
 {
  "file": "scratchpad/pop_probe.py",
  "line": 67,
  "rule": "R6",
  "severity": "style",
  "claim": "The script restores RM.DEG_LIP_SCALE to a hardcoded 16384 instead of saving and restoring the module's actual value, duplicating the shipped default outside its single source of truth.",
  "scenario": "The shipped DEG_LIP_SCALE default is later retuned (24576 was already trialed per the campaign notes); pop_probe then silently leaves the module global at the stale 16384 after running, and any subsequent rendering done in the same interpreter session uses the wrong gate.",
  "needs_code_change": false,
  "unit": "scratchpad-1",
  "verdict": "confirmed",
  "v_needs_change": false,
  "v_reason": "Factually accurate: scratchpad/pop_probe.py:67 restores RM.DEG_LIP_SCALE with a hardcoded 16384 that duplicates the SSOT default at src/doomfj/reference_model.py:185, instead of saving/restoring the original \u2014 if the default is retuned (24576 was already trialed), the restore silently diverges. However the concrete harm is confined to a hypothetical shared-interpreter reuse: the script exits immediately after line 67 (only sheet.save and a print follow), so no rendering ever runs with the stale value in practice. Valid style/R6 note on a throwaway probe outside the certified surface; not worth a code change now."
 },
 {
  "file": "scratchpad/v5spr_debug.py",
  "line": 78,
  "rule": "none",
  "severity": "style",
  "claim": "`im.putdata([pal[c] for c in want] and [])` builds the full palette list and then discards it (a nonempty list `and []` evaluates to `[]`), so putdata is a no-op leftover.",
  "scenario": "No wrong output: the two subsequent im.paste calls cover the whole 2x-wide image, so the discarded putdata only wastes a list construction; if the pastes were ever narrowed, the left/right halves would silently show uninitialized black instead of the intended frame.",
  "needs_code_change": false,
  "unit": "scratchpad-2",
  "verdict": "confirmed",
  "v_needs_change": false,
  "v_reason": "Code is at line 77 (not 78) of scratchpad/v5spr_debug.py, but the claim is otherwise exact: `[pal[c] for c in want] and []` evaluates to `[]` (nonempty list and-chained), so putdata is a no-op that only wastes building the VW*VH palette list. The two paste calls at line 80 fully tile the VW*2 x VH canvas, so output is unaffected. Minor overstatement in the scenario: Image.new defaults to black, so narrowed pastes would expose defined black, not uninitialized data \u2014 and the full putdata would have painted wrong geometry on the 2x-wide canvas anyway, so the `and []` may be an intentional disable. Throwaway scratchpad diagnostic, style-only, no fix warranted."
 },
 {
  "file": "scratchpad/v4_col.py",
  "line": 43,
  "rule": "none",
  "severity": "style",
  "claim": "The script selects the newest scratchpad/fjmcache/v4_*.fjm by mtime with no check that its build flags match the oracle call below, so a stale or differently-flagged cached binary is silently compared against a things+steps oracle.",
  "scenario": "After running v4_check.py with --nothings or --reconly (which caches a differently-flagged .fjm), v4_col.py picks that binary and prints spurious oracle/fj pair mismatches for the requested column, sending a debugging session down a false trail.",
  "needs_code_change": false,
  "unit": "scratchpad-2",
  "verdict": "confirmed",
  "v_needs_change": false,
  "v_reason": "Confirmed against the code: scratchpad/v4_col.py:46-47 picks the newest scratchpad/fjmcache/v4_*.fjm by mtime with no flag check, while its oracle call (lines 58-61) hardcodes things=True/near_steps=True. v4_check.py's cache key (build(), lines 79-86) hashes FLAGS+ABL, so a --nothings or --reconly run writes a differently-tagged, newer .fjm that v4_col.py will then silently compare against the full-flags oracle, producing spurious pair mismatches \u2014 the printed binary name is an opaque hash and cannot reveal the flag mismatch. The cited line (43) is off by three (actual anchor 46-47), but the substance holds. It is a scratchpad diagnostic footgun with no impact on the certified renderer, so per the contract it is a style/tooling note: needs_code_change=false."
 },
 {
  "file": "C:/Users/tomhe/Documents/doom-flipjump/scratchpad/walk_trace.py",
  "line": 64,
  "rule": "none",
  "severity": "style",
  "claim": "The dedup pass collapses ALL consecutive equal events, so a seg genuinely visited twice in a row (immediate revisit across the SET/CLEAR pair boundary) is reported as one visit, understating the walk order it exists to compare against the oracle.",
  "scenario": "If the fj walk enters seg N's ts block, leaves, and re-enters it before any other seg's block fires (4 consecutive events for seg N instead of 2), the printed order shows seg N once \u2014 the fj-vs-oracle order comparison would miss a duplicated visit, the exact class of divergence this tracer was built to find. Counting run lengths (or asserting each run length == 2) would make it exact.",
  "needs_code_change": false,
  "unit": "scratchpad-3",
  "verdict": "confirmed",
  "v_needs_change": false,
  "v_reason": "Verified in scratchpad/walk_trace.py: line 61 strips TSLEAF events before dedup, and lines 62-65 collapse ANY run of consecutive equal seg ids to one entry \u2014 so an immediate re-visit (4 events: N,N,[TSLEAF],N,N) prints identically to a single visit (2 events), silently hiding a duplicated-consecutive-visit divergence, the exact anomaly class this fj-vs-oracle walk tracer exists to catch. Only the raw event count on line 60 could reveal it, and the script never reconciles it. The claim holds as written. However, this is a scratchpad diagnostic tool that never runs in the certification gates, each seg's ts block fires once per frame in the certified walk, and the tool served its campaign; asserting run-length==2 would be a nice hardening but this is a style-tier note on a throwaway tracer, not a defect requiring a fix now."
 }
]